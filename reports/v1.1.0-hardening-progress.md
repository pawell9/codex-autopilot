# Codex Autopilot v1.1.0 hardening progress

**Date:** 2026-09-18

**Branch:** `codex/v1.1.0-hardening`

**Baseline:** `642dc87`

**Status:** Phases A, B, C, and D — COMPLETE. Phase E has not started.

## Evidence boundary

The frozen evidence archive is `/Users/pawell_9/Documents/Autopilot Astra-review/autopilot-hardening-evidence.zip` (SHA-256 `3ff2fd367680aa2066007fe66c2ded8f7835561698e4dc4a9576f6e283dc5772`). The full archive remains external and read-only; it is not committed. The sanitized regression copy is under [`tests/fixtures/legacy/idea-scout-r58/`](../tests/fixtures/legacy/idea-scout-r58/). Live Idea Scout was not accessed.

The unchanged review inputs are in [`reviews/2026-09-18-autopilot-v1.1/`](../reviews/2026-09-18-autopilot-v1.1/), especially the [master hardening plan](../reviews/2026-09-18-autopilot-v1.1/2026-09-18-autopilot-v1.1-master-hardening-plan.md), the [Astra hardening review](../reviews/2026-09-18-autopilot-v1.1/astra-autopilot-hardening-review.md), and the [Astra reliability review](../reviews/2026-09-18-autopilot-v1.1/astra-autopilot-reliability-review-2.md). The approved state contract remains authoritative; this report does not define new architecture.

## Phase A traceability

| Review requirement / finding | Phase A implementation | Regression coverage | Commit |
| --- | --- | --- | --- |
| `AH-07` / master `P0-7` — immutable storage and publication safety | Hash-verified object reads, immutable object publication, pre-fence checks, transactional write planning, and observable snapshot/publication recovery in `tools/ledger.py` | [`tests/test_storage_safety_v110.py`](../tests/test_storage_safety_v110.py): rejected amend leaves canonical bytes unchanged; hash/namespace/type checks; conflicting object protection; exact retry after snapshot failure | `3a4d623` |
| `AH-08`, `REL-10` / master `P0-8` — v1.1 semantic writer floor and legacy write fencing | Semantic contract and minimum-writer metadata in `tools/ledger.py` and `schemas/contracts.schema.json`; legacy state remains readable, while mutation requires explicit migration | [`tests/test_semantic_writer_floor_v110.py`](../tests/test_semantic_writer_floor_v110.py), plus legacy publication/manual-import compatibility regressions in `tests/test_design_publication.py`, `tests/test_initial_intent.py`, `tests/test_requirements_adoption.py`, and `tests/test_wave1.py` | `0dc9ecf`; compatibility coverage repaired in `62faf3b` |
| `AH-08`, `REL-10` / master `P0-8` — read-only legacy diagnostics | Added `diagnose-legacy` analysis for selected legacy ledgers, including R58’s accepted input/producer intersections, candidate/review facts, blocker, and stale next action; it does not inspect referenced paths or mutate the source | [`tests/test_legacy_diagnostics_v110.py`](../tests/test_legacy_diagnostics_v110.py): repeatable read-only output, selected-file-only access, malformed/invalid diagnostics | `5c553fd` |
| `master-plan invariant` — preserve frozen R58 evidence and make rehearsal append-only | Sanitized byte-checked R58 fixture, provenance manifest, and fixture-only append-only rehearsal; no production migration is implemented or claimed | [`tests/test_idea_scout_r58_fixture.py`](../tests/test_idea_scout_r58_fixture.py): archive/artifact hashes, revision and finding counts, 13 intersections, and unchanged fixture bytes | `c5777a0` |

The traceable local commits, in order after baseline, are:

1. `c5777a0` — add sanitized Idea Scout R58 fixture.
2. `3a4d623` — fence immutable storage publication.
3. `0dc9ecf` — enforce the v1.1 semantic writer floor.
4. `5c553fd` — add read-only legacy state diagnostics.
5. `62faf3b` — preserve legacy manual-import coverage across the writer floor.
6. `338e23d` — add canonical Phase B state projections.
7. `5adbbcc` — cover Phase B projections and transitions.
8. `368361d` — record Phase B hardening completion.
9. `ee81b4d` — complete Phase C repair and attempt lifecycle hardening.
10. `e92fb34` — complete Phase D effect and candidate hardening.

## Phase B traceability

| Master-plan item | Phase B implementation | Regression coverage | Commit |
| --- | --- | --- | --- |
| 6 — explicit current candidate | Added canonical `candidates[]`, `ticket.current_candidate`, producer/parent/base/quality/supersession metadata, and dashboard selection from the explicit pointer. Legacy attempt-only fallback is visibly ambiguous and populated pre-marker ledgers remain read-only pending migration. | [`tests/test_phase_b_projections_v110.py`](../tests/test_phase_b_projections_v110.py): pointer independence, no-candidate attempts, continuation fencing, dashboard projection, and legacy mutation fence | `338e23d`; tests in `5adbbcc` |
| 7 — finding / obligation projection | Added a pure current/history/superseded/unbound projection, ticket-scoped verification obligations, exact accepted-review resolution evidence, and issue mirrors. A multi-ticket finding is not globally closed by one ticket's repair. | Phase B projection tests plus updated v1.0.4 and 40-ticket qualification fixtures; linked and unlinked review-verdict regressions | `338e23d`; tests in `5adbbcc` |
| 8 — typed derived `next_action` / allowed events | `next_action` is derived from durable attempts, candidates, findings, obligations, leases, effects, and lifecycle state. Emitted actions carry typed event IDs; terminal actions carry no executable event; internal waits remain explicitly non-user checkpoints. | Phase B action/executability tests, initial-intent compatibility, lifecycle repair tests, and qualification runs | `338e23d`; tests in `5adbbcc` |
| 9 — shared phase/control/terminal transition table | Added shared event-by-control and event-by-phase admission tables plus phase/control transition tables. Terminal state is immutable across mutators; G2/G3/G6 identity is explicit; BLOCKED repair and RECOVERING resume exceptions require exact proof. | Phase/control admission, terminal mutator, explicit gate, recovery-fence, design publication, Wave1/Wave2, and qualification regressions | `338e23d`; tests in `5adbbcc` |

## Phase C traceability

| Master-plan item | Phase C implementation | Regression coverage | Commit |
| --- | --- | --- | --- |
| 10 — canonical `RepairPlan` / `AttemptPlan` | Added immutable candidate-bound `RepairPlan` and packet/route/base/epoch/intent/write/check/risk-bound `AttemptPlan` objects. Legacy singleton authorization remains explicitly marked as unbound compatibility state. | [`tests/test_phase_c_repair_plans_v110.py`](../tests/test_phase_c_repair_plans_v110.py): exact plan binding, drift rejection, stale/forked source and base rejection, and compatibility behavior | `ee81b4d` |
| 11 — grouped `finding_refs[]` | Added bounded grouped repair contracts with one hypothesis/proof pair per finding. Mixed-ticket, historical-candidate, duplicate, and incompletely proven groups reject without mutation. | Grouped three-finding authorization/dispatch, mixed-subject rejection, schema bounds, and exact finding-set consumption | `ee81b4d` |
| 12 — shared authorize/dispatch preflight | Authorization and dispatch now share canonical source/base/scope/dependency/deny/route/epoch checks. Packet-bound authorization is single-use with `authorized → consumed`, plus explicit unused revoke/supersede and exact replay. | Phase C repair tests plus legacy lifecycle/Wave2 compatibility; wrong-ticket return and allow/deny overlap reject before state publication | `ee81b4d` |
| 13–14 — general attempt finalization and continuation/no-change/lost/interrupted/quarantine paths | Added `finalize-attempt` for returned `BLOCKED`/`HANDOFF`/`FAILED` and terminated `LOST`/`INTERRUPTED`. Clean no-change keeps the explicit prior `DONE`/`CONTINUATION` candidate or returns an initial ticket to its verified base; partial/foreign/unknown state never becomes `DONE`. Added `reconcile-finalized-attempt` for late PASS stop+cleanup proof and exact baseline disposal, with zero-effect failed proof and immutable replay. | [`tests/test_phase_c_attempt_finalization_v110.py`](../tests/test_phase_c_attempt_finalization_v110.py): full non-DONE matrix, continuation retention, initial retry, lost/interrupted release, quarantine, late reconciliation, replay after progress, and no accidental success promotion | `ee81b4d` |
| 15 — source/provenance normalization | Review-sourced repair findings normalize to the authoritative current candidate producer; transitive create/modify/preserve lineage uses the common normalization and is rechecked at candidate preservation/publication boundaries. | Phase C source normalization, existing transitive lifecycle repairs, and continuation candidate regressions | `ee81b4d` |

## Phase D traceability

| Master-plan item | Phase D implementation | Regression coverage | Commit |
| --- | --- | --- | --- |
| 16 — typed effect state machine | Added the guarded lifecycle `prepared → applied/uncertain/abandoned`, `uncertain → applied/abandoned`, and candidate `applied → finalized`. Prepared, uncertain, and applied-but-unlinked candidate effects remain pinned and derive a typed safe action; abandoned/finalized IDs cannot be re-armed. | [`tests/test_phase_d_effects_v110.py`](../tests/test_phase_d_effects_v110.py): fresh uncertain resolution, abandonment evidence, exact/conflicting replay, snapshot pinning, and typed next actions | `e92fb34` |
| 17 — applied-effect adoption/finalization | A real Git commit recorded as applied is adopted from its immutable receipt and finalized atomically with candidate linkage, without a second commit. Exact replay is zero-effect and cross-state links are validated as one unit. | Real post-commit/pre-ledger crash simulation, applied receipt replay/conflict tests, commit-count assertion, and finalized link-tamper tests | `e92fb34` |
| 18 — shared `VerifiedCandidateProof` | Ordinary DONE and BLOCKED/HANDOFF continuation publication now share one hash-addressed proof over run/ticket/attempt/operation identity, packet/return, exact base/direct parent/commit/tree, checkout/target/authority, original receipt, full write-set audit, and candidate quality. | [`tests/test_phase_d_candidate_proof_v110.py`](../tests/test_phase_d_candidate_proof_v110.py): proof-shape parity, immutable object retention, missing-object failure, receipt binding, and cross-record invariants | `e92fb34` |
| 19 — strong ordinary candidate publication | Ordinary candidates now use the same real Git and full tracked/untracked/ignored/rename/type/mode/symlink audit boundary as continuations. Wrong identity, base, target, authority, parent, checkout, tree, foreign path, or missing proof rejects before publication. | Phase D proof tests, existing continuation/Wave1 regressions, v1.0.4 lifecycle qualification, and the real-Git 40-ticket qualification | `e92fb34` |

## R58 facts verified from the frozen fixture

- Revision `58`; phase/control `EXECUTE / BLOCKED`.
- The accepted publication has 13 input/producer intersections across 9 tickets.
- Current `T02-R17-REVIEW-CODE-05` has 3 findings; historical T02 code reviews have 7 findings; historical design reviews have 11.
- `T02-R17-WORKER-05` is the continuation candidate producer; candidate SHA is `ef8da4ff49cc7f8014d90f463acf8319ad7c1335`.
- External blocker: `issue-62c0073109c9529a`.
- Stored `next_action` is stale (`await_blocked_candidate_review`) and points to returned attempts.
- The append-only rehearsal test leaves all original fixture artifact bytes unchanged. It is not a production migration/recovery rehearsal.

## Verification and limits

Recorded Phase A verification: storage-focused suite — 72 passed; writer-compatibility suite — 40 passed; diagnostics suite — 17 passed; its final full suite contained 112 tests with one documented opt-in skip.

Recorded Phase B verification: `python3 -m unittest discover -s tests -v` — `142 tests, OK, skipped=1`, with no failures. The skip remains the opt-in legacy production-checkpoint audit; normal tests use sanitized fixtures. Focused Phase B projection coverage passed 28/28; the final independent architecture review returned `SHIP`. The v1.0.4 and 40-ticket qualification tests passed 2/2; the final isolated 40-ticket run completed in 82.907 seconds against its `<90s` assertion. `py_compile`, JSON schema parsing, and `git diff --check` passed.

Recorded Phase C verification: focused repair/attempt lifecycle suite — `44 tests, OK`; final `python3 -m unittest discover -s tests -v` — `159 tests, OK, skipped=1`, with no failures. The skip remains the documented opt-in legacy production-checkpoint audit. The v1.0.4 qualification passed; the isolated 40-ticket qualification passed in 84.814 seconds against its `<90s` assertion and also passed inside the final full suite. `py_compile`, JSON schema parsing, and `git diff --check` passed.

Recorded Phase D verification: dedicated effect/candidate modules — `11 tests, OK`; focused Phase D plus continuation/Wave1/model/v1.0.4 qualification — `31 tests, OK`; final `python3 -m unittest discover -s tests -v` — `170 tests in 348.744s, OK, skipped=1`, with no failures. The skip remains the documented opt-in legacy production-checkpoint audit. The 40-ticket qualification now performs 40 real Git candidate commits and complete candidate-proof audits and passed inside the final suite under its `<180s` guard; v1.0.4 qualification also passed. `py_compile`, contracts/lifecycle JSON parsing, and `git diff --check` passed.

This is not a claim that v1.1.0 is release-ready or that later hardening phases are complete. Reviews/integration hardening remains in **Phase E**, which has not started. An explicit production migration/recovery workflow remains in **Phase F — Legacy R58 compatibility**. No live R58 migration, recovery, or product mutation is claimed. Continue with Phase E only in a subsequent session.

Current local commits are listed above; they have not been pushed.
