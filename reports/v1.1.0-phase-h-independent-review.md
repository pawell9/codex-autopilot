# v1.1.0 Phase H independent diff/state-machine review

**Reviewer:** fresh independent reliability reviewer
**Branch:** `codex/v1.1.0-hardening`
**Phase G baseline:** `34c328c`
**Code HEAD reviewed:** `d58b176` (`chore: refresh v1.1.0 content manifest`)
**Review date:** 2026-09-19
**Review commits in scope:** `af40463`, `b513b02`, `31fe9eb`, `8f6d252`,
`d7605e9`, `ebc2d01`, `b10cf21`, `9413da8`, `f924c59`, `17ceee1`,
`b30c432`, `d251ca9`, `d58b176`.

The review used the master hardening plan and Phase H/DoD, the canonical Q01–Q42
matrix in both Astra artifacts (`astra-autopilot-hardening-findings.json` and
`astra-autopilot-reliability-findings-2.json`), the Phase H progress report,
and the durable handoff. No GSD workflow was used. Live Idea Scout/R58 state
was not accessed or mutated.

## Verdict

**PASS.** H-01 through H-07 are closed on the current source and executable
qualification evidence. Q01–Q42 now run as real disposable Git/CLI scenarios,
the two previously blocked canonical gaps are closed, fail-closed product
resolution remains covered, and release/source/runtime/native boundary claims
are explicit. The global discovery suite remains the orchestrator's gate and
was intentionally not run in this independent review.

## Finding disposition

- **H-01 / Q25: CLOSED.** Full Q25–Q42 execution passes crash/fault injection
  at object-store, `ledger.prev`, ledger, snapshot, receipt-object,
  runtime-observation, and Git-before-receipt boundaries with exact retry and
  recovery assertions.
- **H-02 / Q21: CLOSED.** The current Q01–Q24 matrix executes independent PASS
  and BLOCK continuation review paths and rejects direct integration with
  byte-preserving rejection behavior.
- **H-03 / Q19: CLOSED.** Q19 executes continuation repair, no-change
  retention, finalization, blocker/action/lease checks, and replay.
- **H-04 / Q28: CLOSED.** Q28 executes one preserve hop, two distinct
  candidate-bound repair authorizations/commits, exact finding and external
  blocker evidence resolution, immutable qualification-ref integration, and a
  negative unrelated-target/evidence case.
- **H-05 / Q30: CLOSED.** Q30 executes conflicting and stale reviews and keeps
  integration blocked until currentness/disagreement barriers are satisfied.
- **H-06 / Q35: CLOSED.** Q35 now uses the shared `V104Qualification`
  bootstrap/design cycle, then performs public `ready-ticket`, `dispatch`,
  runtime observations, review ingest, repair authorization, repair dispatch,
  repair ingest, effect preparation, and public preserve transitions. It
  asserts persisted source worker, source finding/review, authorization,
  candidate/base, expanded lease, lineage, receipt, parent, and continuation
  references; no provenance fallback remains.
- **H-07 / Q03: CLOSED.** Q03 now has one continuous HANDOFF preserve hop,
  two candidate-bound repair authorizations, two repair workers, a sentinel
  path that is created then left untouched by both repairs, exact repair
  provenance/lineage/packet/deny/parent assertions, findings projection and
  resolution, and final qualification-ref integration.

## Shared bootstrap and public-transition audit

The Q35 bootstrap is not bespoke hidden ledger setup. It instantiates the same
`experiments.v104_lifecycle_qualification.Qualification` used by Q01–Q24,
runs its shared `bootstrap()` and `design_cycle()`, and changes only the
generated design bundle's T-1 zone before the public design publication so the
create→modify provenance case is executable. The shared bootstrap's one
checkout-binding transaction is the documented fixture bootstrap; its remaining
intent, recovery, requirements, reviews, publications, and gates use public
CLI transitions.

After `q.design_cycle()` in Q35, a static audit of the scenario finds no
`ledger.atomic_write`, `ledger.transaction`, `ledger.validate_ledger`, direct
state assignment, or pointer edit. State changes use `q.mutate(...)` or
explicit public `run(...)`; Git commits and evidence/packet/return files are
external worker/reviewer effects, not ledger fabrication. Q03 has the same
shared bootstrap boundary and only public lifecycle transitions afterward.

## Canonical Q03/Q35 evidence

Q03 (`tests/test_phase_h_qualification_matrix_v110.py:424–580`) proves:

- `untouched-sentinel.txt` is created by Q03-W1 and byte/hash-identical after
  Q03-W2 and Q03-W3; repair returns contain only the changed application path.
- Both repair packets allow the application and sentinel modify paths, deny
  `forbidden.txt`, and carry the exact prior worker source attempt.
- Q03-W2 and Q03-W3 each persist `repair_lease_provenance` with exact
  authorization, finding, candidate/base SHA, expanded entries, and per-path
  lineage. Lineage records `create`, `modify`, and `preserve` operations as
  applicable.
- Candidate parents are exactly `candidate-Q03-W1` then `candidate-Q03-W2`;
  both authorizations are consumed once by the intended worker.
- A fresh candidate-bound PASS qualification resolves both historical findings
  and the external continuation blocker through exact evidence, closes the
  projection obligations, releases all ticket leases, and integrates Q03-W3.

Q35 (`tests/test_phase_h_boundaries_v110.py:619–808`) proves the create-only
  ticket case through public worker/review/repair/preserve transitions. The
  repair attempt's durable provenance names the normalized source worker A-1,
  exact source candidate/base, review finding, repair authorization, expanded
  path, create-origin lineage, repair return, and final continuation receipt.
  Tampering with or deleting the stored continuation object is rejected without
  a second publication.

## Product, invariants, and boundary review

- **Fail-closed resolution:** `tools/ledger.py:8125–8293` requires accepted
  returned PASS reviews, exact current candidate fingerprints, immutable return
  equality, unique current ticket findings or typed external blockers,
  continuation receipt binding, exact external check/criterion evidence, no
  remaining obligations/blockers/effects, then projects resolution and issue
  mirrors atomically. Phase E, Phase B projection, and Q03 integration tests
  pass.
- **Replay/fault injection:** Q25–Q42 passes exact/conflicting replay,
  object-tamper, Q30 disagreement/staleness, Q35 object deletion/tamper, and
  Q41 deny/identity/write-scope barriers.
- **Architecture/backward compatibility:** explicit candidate/producer
  pointers, lease/liveness separation, immutable review/qualification facts,
  candidate proof, and copy-only legacy R58 compatibility remain intact.
- **Release/source/runtime parity:** the refreshed `d58b176` content manifest
  is accepted. Source/install parity covers 21 runtime files; fake runtime
  replay is deterministic and idempotent; native runtime is explicitly
  `UNSUPPORTED`/adapter-bound rather than claimed as proven.
- **Live R58:** no live path or frozen source artifact was accessed or changed;
  Phase F remains copy-only/read-only.

## Verification evidence

- `python3 -m unittest tests.test_phase_h_qualification_matrix_v110 -v` —
  **24/24 passed** in 332.477s (current Q01–Q24, including Q03).
- `python3 -m unittest tests.test_phase_h_boundaries_v110 -v` — **20/20
  passed** in 183.543s (current Q25–Q42, including Q25/Q28/Q30/Q35/Q41 and
  manifest completeness).
- Focused Q03 — **1/1 passed** in 66.947s; focused Q35 — **1/1 passed** in
  53.001s (wall time includes concurrent workload).
- Relevant Phase C/D/E/G regressions — **36/36 passed** in 200.154s.
- Phase B projections, continuation candidates, and lifecycle repairs —
  **55/55 passed** in 115.235s.
- `python3 -m unittest tests.test_phase_h_release_parity_v110 -v` — **9/9
  passed** in 1.269s.
- `python3 experiments/v110_release_qualification.py` — qualified 21
  source/install files, 110 content files, 74 fixture files, 33 test modules,
  and 3 qualification scripts; isolated parity and fake runtime passed;
  native runtime explicitly `UNSUPPORTED`.
- `python3 -m unittest tests.test_phase_f_legacy_r58_v110 -v` — **5/5
  passed** in 33.427s; rehearsal remained copy-only/read-only.
- `python3 -m py_compile tools/ledger.py tests/test_phase_h_boundaries_v110.py
  tests/test_phase_h_qualification_matrix_v110.py experiments/v104_lifecycle_qualification.py
  experiments/v110_release_qualification.py` — passed.
- `git diff --check 34c328c..d58b176` — passed.
- Full global discovery was intentionally deferred to the orchestrator; no
  global-suite result is claimed here.

## Definition-of-Done checklist

| Master-plan DoD item | Status | Independent evidence / limitation |
| --- | --- | --- |
| All P0 closed | **PASS** | Current matrix and Phase C–G regressions pass; no open canonical finding remains. |
| All P1 closed for general release | **PASS** | Q01–Q42, boundary regressions, package parity, and explicit runtime limits pass. |
| Q01–Q42 implemented and passing | **PASS** | 24/24 plus 20/20 current matrix runs. |
| Astra negative probes fail/recover safely | **PASS** | Matrix, Phase C/D/E/G, Q25, Q28, Q30, Q35, Q38, and Q41 negative paths pass. |
| Existing idempotence controls remain no-op | **PASS** | Q14/Q15/Q19/Q21/Q25–Q35 and parity replay controls pass. |
| Fault injection at every durable boundary | **PASS** | Q25 covers the current durable boundary set with exact recovery/retry assertions. |
| Complete source/test/fixture package reproduces CI | **PASS for package gate** | Release qualification inventory and isolated install parity pass; global discovery is deferred to orchestrator. |
| Source/runtime parity | **PASS** | 21-file source/install hashes and runtime version checks pass. |
| Legacy R58 read-only rehearsal | **PASS** | 5/5 Phase F tests; no live state mutation. |
| Raw returns/publications/predecessor history untouched | **PASS** | Review changed only this report; no product/test/R58 artifact edits. |
| Ambiguous legacy semantics remain blocked | **PASS** | Phase F and product guards preserve explicit blocking semantics. |
| Native runtime limits documented/tested | **PASS** | Fake runtime supported; native boundary explicitly `UNSUPPORTED`. |
| Final independent diff/state-machine review | **PASS** | This report is the independent current-HEAD review. |

## Residual limitations and release disposition

The native runtime is an explicit adapter boundary: the package proves the
fake runtime and ledger-side receipt checks, not truthfulness or completeness
of an external native observer. Global test discovery is also intentionally
owned by the orchestrator. These are documented limits, not hidden release
claims. No material Phase H qualification gap remains in the reviewed source.

**Release disposition: PASS, subject to the orchestrator's global discovery
gate and normal release-owner approval.**
