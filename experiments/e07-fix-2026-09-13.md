# E07 targeted blocker fix — 2026-09-13

## Change

`cmd_gate` now permits `PAUSED` only when:

- the current control is `QUIESCING`;
- no attempt is `PREPARED` or `DISPATCHED`;
- no attempt has an `active` or `quarantined` lease; and
- no operation is `prepared` or `uncertain`.

No other lifecycle semantics were changed.

Implementation: [`tools/ledger.py:1729-1745`](../tools/ledger.py#L1729).

## Verification

- Regression added:
  [`test_pause_cannot_bypass_quiesce_with_active_attempt_or_prepared_effect`](../tests/test_wave1.py#L77).
- Current regression suite: **16/16 PASS**.
- E07 repeat: **PASS**.

E07 repeat observations:

- direct `ACTIVE → PAUSED` rejected;
- safe `QUIESCING → PAUSED → RECOVERING → ACTIVE` completed;
- normal disposable micro-project reached `ACCEPTED` through manual G5/G6;
- green-but-broken oracle produced `BLOCKED`, not acceptance;
- cancellation preserved foreign files;
- amendment moved the ticket to `STALE` and recorded invalidation closure.

Package SHA-256 after the fix:

```text
tools/ledger.py  9037d7a8b625bdcb0d7eaa9a41bf5db3efc28e76dc345e02ac902389d1e513f7
```

E09, E10, and global installation were not run.

## Result

**`E07 FIXED`**
