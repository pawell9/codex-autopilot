# E03-M resumable run — first half

Status: waiting for independent user-assisted reviewer; do not import or
simulate a reviewer in this session.

## Durable state

- Run: `e03m-manual-g5-2026-09-13`
- Control root: `experiments/fixtures/e03m/seed-repo`
- Phase/control: `VERIFY × BLOCKED`
- Ledger revision: `7`
- Lifecycle reason: `manual_review_pending`
- Next action: `import_manual_review`
- Attempt: `A-E03M-WORKER-1`
- Candidate commit/tree: `0a5d2b78af78411f7c6b71405e262092b78da1eb` / `d687e71ab613ea65a1ec578436a8a2662ab546ec`
- Intent revision/hash: `intent-v1` / `451bfd6693410a5f757cdff4646f1de6304fc7119a0f987c0e89e6b3b3270529`
- Packet hash: `94623e39528f122a4227bd5db6c60409047fcfa17d8204143c1f1f1e283b683b`
- Projection hash: `79aedaeec289fb3cb43e0b355c63801c8cc1a1b70ce135c1bc13aff0c724ac7e`
- Export manifest hash: `3698d5ed9dfb344d4c2228da5cdbba2840dabc44eec889fea141cf9a1a70d98a`
- Pre-review ledger hash: `0c59a461195d88ff8020615de668651199dfb578ef420a4bfce7e954a7c18e5c`

## Verified before handoff

The worker return was accepted through the exact registered attempt inbox;
write-set audit passed with only `app.txt`; candidate commit receipt passed;
the candidate export was produced from the candidate commit and its three
manifested files match byte-for-byte; packet and projection source bytes match
the generated bundle bytes; the projection has exactly one active criterion;
the embedded acceptance return schema is present; no environment/context
receipt was fabricated.

The helper accepts `kind=acceptance` for handoff but has no separate
`acceptance_packet` schema definition. This is recorded as an implementation
observation, not repaired in the experiment.

## Resume contract

After the user returns exact structured reviewer evidence plus valid environment
and context receipts, resume by re-reading `SKILL.md`, `phases/accept.md`,
`references/ledger.md`, and `references/safety.md`; verify the run is still at
revision 7 with the same candidate/intent; then invoke the production
`import-manual` path with the recorded integrity receipt. Do not run another
experiment or release test.

## Reviewer return audit — blocked

Reviewer artifacts arrived in `/private/tmp/e03m-review-workspace.zzqPTK`.
Acceptance return SHA-256 is
`776d55c2eb9ceb089f7e93a10113cd2930d32eddcca0b8d9ddaea6709ad8571a`; it is
schema-valid and reports `PASS` with fulfilled `C-E03M-1`. Candidate fingerprint,
intent revision, criterion ID, candidate export manifest, packet hash, and
integrity receipt all match the frozen subject. Candidate export remained
byte-for-byte unchanged.

Production validation rejected `environment-receipt.json`: missing
`receipt_id`, `status`, and `topology`, with `inventory_hashes` also shaped as
an object instead of the required array. It rejected `context-receipt.json`:
missing `receipt_id`, `status`, `packet_hash`, `export_hash`, `clean_input`, and
`contamination_absent`. The reviewer return also has `identity.packet_hash=null`,
while the prepared attempt requires packet hash
`94623e39528f122a4227bd5db6c60409047fcfa17d8204143c1f1f1e283b683b`.

The exact production `import-manual` invocation failed before publication with
`environment receipt is not PASS`. Ledger revision, lifecycle, candidate, and
integrity baseline remained unchanged. No reviewer result or receipt was
rewritten and no PASS was substituted.

## Retry reviewer return — final E03-M blocker

The retry artifacts were received from `/Users/pawell_9/Downloads`:

- acceptance return SHA-256:
  `b4f3516b2155770c511962b40878e4043e6f373238a02f485852bf4018c19dc3`
- environment receipt SHA-256:
  `e7567709270cad99d980eb9242ad829213a961830f4cb3ec7b302265dade8e1d`
- context receipt SHA-256:
  `9e74d1252b6e2dd68b0fb424ef7ce87601ac2238f5b0414663d01543d6c7fc12`

Acceptance return and context receipt are formally schema-valid. Packet,
projection, manifest/export, candidate commit/tree, intent/criterion identity,
and integrity baseline all match the frozen subject. The reviewer returned
`BLOCK` with one blocking `reviewer-environment` finding. The environment
receipt is schema-invalid for import because `status=BLOCK` rather than `PASS`
and `authoritative_absent=false`; the finding records credential-named
environment variables and failed attempts to establish a narrower kernel
filesystem boundary. The production `import-manual` call rejected it with
`environment receipt is not PASS` before any ledger publication.

Final experiment verdict: `E03-M BLOCKED`. The ledger remains revision 7 at
`VERIFY × BLOCKED` with reason `manual_review_pending`; no candidate, fixture,
production implementation, reviewer evidence, or receipt was rewritten.

## Contract/policy qualification finding and targeted revision

Finding classification: `contract/policy`, not `candidate` and not
`implementation`. The prior reviewer could not prove full underlying-host
filesystem isolation or absence of all credential-like host environment
variables, facts outside the browser reviewer's reliable observable boundary.
That evidence gap does not show a candidate defect.

Targeted revision preserves the new independent session, bundle/export-only
transfer, immutable candidate, packet/export/manifest/hash checks, clean-input
attestation, exact structured return, independent oracle evidence, and the
distinction between `MANUAL_ATTESTED_CLEAN` and `STRICT_FRESH`. It removes only
the requirement to prove unobservable host-wide properties. Known authoritative
project input access, connector use, credential use, contamination, stale or
tampered subject, missing oracle, and plain user approval remain blocking.

Repeat package for the same frozen subject:
`experiments/e03m-manual-g5-policy-revised-repeat-2026-09-13.md`.
No ledger transition or reviewer execution has been performed for the repeat;
the current run remains resumable at `manual_review_pending`.

## Policy-revised repeat import and G6 disposition

The external reviewer returned new files from `/Users/pawell_9/Downloads`.
They were copied byte-for-byte into the registered attempt inbox and imported
through the production `import-manual` command; the Downloads sources were not
modified.

- acceptance return SHA-256:
  `5c9947ba8987989eca6eb71082b453d8d878345965e20dc04baac85de368454c`
- environment receipt SHA-256:
  `53c4d51c0f195c884b9d64e707384eb2e985833715fe9dba29e489d64950e50d`
- context receipt SHA-256:
  `9c16a5b84a7e5c52f2debcc883d241d0d9c650cd8aeca695d8f49029ece83ceb`

Import checks passed: acceptance-return schema and exact top-level shape,
run/attempt/packet/intent identity, candidate fingerprint, `PASS` verdict,
fulfilled `C-E03M-1`, environment inventory/probes and scoped
`authoritative_absent`, `MANUAL_ATTESTED_CLEAN` context, packet/export hashes,
and the pre-review integrity barrier. Production import published revision 8
with an independent manual G5 `PASS`.

G6 was not recorded. The existing ledger still contains
`attempt.lease.state=active` and ticket `T-E03M-1` in `REVIEW`; the contract
requires closed leases/blockers before `ACCEPTED`. Revision 9 records the
exact resumable blocker `g6_precondition_active_manual_attempt_lease` without
changing the candidate, bundle, export, reviewer evidence, or production
implementation.

Final experiment verdict: `E03-M BLOCKED` — manual G5 roundtrip/import passed,
but the existing lifecycle cannot be finalized safely until the active lease
is reconciled without changing the frozen candidate.

## Targeted orchestration repair and final G6

Existing commands were checked before changing production code. `gate` only
updates lifecycle fields; `integrate` requires a separate change-review return;
`recover` quarantines rather than closes a lease. None could correctly finish
this manual-acceptance path.

Targeted orchestration defect: `import-manual` recorded a valid manual G5 PASS
but did not release the associated attempt lease or advance its REVIEW ticket.
The minimal fix in `tools/ledger.py` now performs that reconciliation for a
manual PASS, including the idempotent import path. No candidate, reviewer
return, packet, projection, manifest, or export was recreated or changed.

The same reviewer return was re-imported idempotently using the post-import
integrity receipt, which preserves all frozen candidate/packet/export hashes.
The attempt lease became `released` and `T-E03M-1` became `INTEGRATED` at
revision 10. G6 then published `ACCEPTED` at revision 11.

Final experiment verdict: `E03-M PASS`.
