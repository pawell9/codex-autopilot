# Остановка волны 4 — 2026-09-06, ночь

Остановлено по просьбе пользователя на логической точке: **ничего не слито
в `main`, оба таска живут в своих worktree, контракт зафиксирован.**

## Где ровно остановились

| Что | Состояние |
|---|---|
| `main` | `93a4f22`, дерево чистое. Кода тасков 09 и 10 в нём **нет** |
| Сдано тасков | 8 из 17 (01–07, 16) — как и было до волны 4 |
| Контракт | доведён до **D21** (было D18) |
| Требований закрыто | 13 из 47 — **не менялось**: волна 4 не принята |

## Таск 10 — граница обнаружения

Worktree `<USER_HOME>/Documents/idea-scout-wt/t10`, ветка
`task/10-discovery`, исполнитель — Codex Terra High, сессия жива
(`--resume-last` продолжит её).

- Круг ремонта 2 **сдан и проверен оркестратором**: `249 passed, 5 deselected`,
  `ruff check` и `ruff format --check` чисты. Проверено самостоятельным
  прогоном, не со слов исполнителя.
- Исполнитель заявляет, что закрыты все семь дефектов круга 2. **Ревью этого
  круга не проводилось** — это первое, что нужно сделать при продолжении.
- Не слито. Не проверено живьём (`pytest -m real` по обнаружению требует
  ключа Tavily, которого нет и который пользователь ещё не заводил).

**Что именно проверять в круге 3.** Класс дефекта, повторившийся дважды:
*страж написан, но не сторожит*. В круге 2 он дал семь штук. Ответы
исполнителя на них выглядят верно по описанию, но описанию верить нельзя —
проверяются прямыми прогонами:

1. Потолки при `config=None` (заявлены контрактные умолчания 10/30).
2. `max_usd` и `spend_usd` у Tavily вернулись к $0.008 — денежный потолок
   обязан срабатывать.
3. `_matches_policy` на паре `"CC BY-SA"` / `"CC BY-SA 4.0"`.
4. След запроса пишется **отдельной** транзакцией до `save_raw` и переживает
   откат сохранения; неизвестная стоимость — `NULL` + `spend_known=0`,
   а не ноль.
5. `RemoteProtocolError` уводит резерв в `unknown`; резерв не остаётся
   в `held` ни при каком выходе.
6. Отброшенные `run_discovery_batch` запросы называются.

**Правило эскалации:** это был второй круг ремонта. Если третий провалится
по вине реализации — подъём на Sol High (`model = "gpt-5.6-sol"`) и **свежая
сессия** (`--fresh`, не `--resume-last`): прежний тред может нести
конструкцию, которая и даёт повторяющийся класс. Так было на таске 07.

## Таск 09 — источники MVP

Worktree `<USER_HOME>/Documents/idea-scout-wt/t09`, ветка
`task/09-sources-mvp`, исполнитель — Claude subagent.

- Первый круг сдан: 273 теста, живой сбор по всем четырём источникам прошёл.
- Два ревью дали 26 находок, две критические.
- **Круг ремонта 1 был запущен и на момент остановки не завершился.** Его
  результат придёт в worktree независимо от того, читает ли его кто-то:
  код останется в рабочей копии, ничего не потеряется. При продолжении —
  сначала `git status` и `pytest -q` в этой копии, потом ревью.

**Две критические находки, ради которых шёл ремонт:**

1. **Stack Exchange терял 40 % окна и рапортовал успех.** `boundary` сужался
   после каждой страницы и уходил в запрос как `todate`, пока `page` рос:
   номер страницы отсчитывался по новой выборке. Симуляция настоящего API:
   250 вопросов на входе, 151 собрано, **99 потеряно**, исход `ok`,
   `complete=True`, курсор выше непрочитанного. Образец рядом —
   `hackernews.py:117` держит `upper` неизменным всю пагинацию.
   Тесты этого не видели: `_se_handler` принудительно ставил
   `has_more: False`, то есть все тесты SE были одностраничными.
2. **Полная перепубликация в SQLite** у Stack Exchange и GitHub через
   `raw_payload=dict(hit)` — полный `body` под CC BY-SA и персональные поля
   GitHub сверх логина. Оба паспорта это прямо запрещают. Product Hunt и RSS
   в том же таске сделаны правильно, белым списком.

## Что сделал оркестратор и что осталось за ним

**Сделано и закоммичено:**

- D19 — `retry_after` в `DiscoveryResult`: паспорт требовал того, чего
  контракт не выражал.
- D20 — на какой вопрос отвечает `policy_gate`. Понадобилось, потому что два
  ревьюера прочитали ворота **противоположно**, и это было доказательством
  пробела, а не спором о вкусах.
- D21 — критерий GitHub разделён: `search/repositories` добавлен в паспорт
  живой цитатой из docs.github.com; **discussions сняты из плана волны 4**
  (в REST-поиске их нет, это GraphQL — другой канал без паспорта).
  Требование R10 не снято, снят способ. **Пользователь может это отменить.**
- Три ключа `discovery.*` и метод `Store.discovery_runs_since` объявлены за T10.
- Паспорта: шесть правок (потолок HN, `budget_exhausted` вместо `error`
  у трёх источников, `sort=creation` у SE, маршрут `search/repositories`).
- Секреты зарегистрированы: `TAVILY_API_KEY`, `EXA_API_KEY`,
  `BRAVE_SEARCH_API_KEY` (не заполнять), `GITHUB_TOKEN`, `STACKEXCHANGE_KEY`.
- **Долг таска 07 «сверка цитат условий» закрыт живой выборкой.** Из трёх
  цитат подтвердились две: YC legal дословно (и с ней — основание выбора
  канала: запрет на scraping относится к сайту, поэтому источник идёт через
  Algolia и Firebase) и «There is currently no rate limit» у Firebase API.
  Лимит 10 000 запросов в час у Algolia **не подтверждён**: страница
  по-прежнему пустой каркас SPA. Число оставлено как работающее в безопасную
  сторону, но помечено «не снятая цитата» — сначала цитата, потом частота.

**Осталось за оркестратором при продолжении:**

- Ревью круга 2 таска 10 и круга 1 таска 09 — **теми же двумя постоянными
  ревьюерами**, они живы: `review-manifest`, `review-craft`. Новых не заводить.
- Живой прогон таска 09 после ремонта (`pytest -m real` плюс прямые вызовы
  по краям — именно они, а не тесты, ловят класс «курсор выше непрочитанного»).
- Слияние обоих тасков в `main`. **Ожидается конфликт в `config.py`
  и `.env.example`:** обе ветки дописывали `KNOWN_SECRETS` и свои ключи.
  Разрешать оркестратору, обе правки нужны целиком.
- Таск 08 — **не начат намеренно**, отложен до обновления лимита Codex.

## Открытые вопросы, не блокирующие

- Окно свежести `discovery.freshness_days` не применяется: паспорт не
  подтверждает имя параметра у Tavily, и исполнитель правильно отказался его
  выдумывать. Закрывается живым прогоном с ключом.
- `retry_after` у `FetchResult` (таск 09) — тот же класс, что D19: паспорта
  GitHub и RSS предписывают брать заголовок, а поля нет. **Решения нет**,
  сознательно: `FetchResult` конструируется в 12 местах пяти файлов, и
  трогать его стоит одним ходом вместе с ремонтом, а не отдельно.
- Tavily остаётся рекомендацией; Reddit OAuth, пилот X API и письмо в
  Open Collective не запускались. Таск 17 отложен.
