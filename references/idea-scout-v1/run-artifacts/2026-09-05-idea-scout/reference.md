# Эталон

На что это должно быть похоже. Собрано со слов пользователя — не выдумано.

## Внешний вид
- linear.app — «Плотный список как в Linear» (ответ в брифинге). Строки, а не плитка;
  много идей на экране; карточка раскрывается панелью сбоку, не отдельной страницей.

## Чего быть не должно
- аккаунтов, логинов, прав доступа — «Только я»
