# Манифест требований

Источник: `2026-09-05-brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «Парсер ищет идеи несложных реализаций» | done | целевой класс — софт, который делает один человек; более сложные проекты не отбрасываются, а уходят в отдельный раздел «на потом». **Закрыто 2026-09-07 таском 08:** ось «простота реализации» и `level_incomplete` без профиля не превращают уровень в отказ | spec: Истории 1–2 → T08 (сдан) |
| R02 | «плюс-минус быстрое в производстве агентские системы» | done | тема-тег «агентская система»; входит в целевой класс. Закрыто таском 08: закрытый список из семи тем в `types.THEMES` | spec: История 1 → T08 (сдан) |
| R03 | «какие-то мини-продукты любого формата» | done | тема-теги: бот, расширение, SaaS, агентская система, API-сервис, микроинструмент. Закрыто таском 08 | spec: История 1 → T08 (сдан) |
| R04 | «которые в сети фигурируют и которые выкладывают пользователи» | done | источники — публичные посты, листинги и профили; ничего закрытого не собирается. **Закрыто 2026-09-07:** пять каналов MVP сданы, граница `discovery` не даёт поисковой находке стать содержимым | spec: История 3 → T07 |
| R05 | «имеют подтвержденные результативные показатели, такие как выручка» | done | уровни доверия: verified (самопубликуемый платёжный дашборд) / proof (скрин, листинг) / claimed (только слова); ничего не отбрасывается. TrustMRR как якорь verified выключен до письменного разрешения — G09 | spec: Истории 15–21 → T03 (сдан) |
| R06 | «количество пользователей в месяц, то есть DAU, MAU, если это сервис какой-то» | done | метрики: MRR/ARR, выручка, DAU/MAU, платящие, установки — набор зависит от типа продукта | spec: История 15 → T03 (сдан) |
| R07 | «в зависимости от сервиса свои необходимые показатели» | done | у карточки хранится тип продукта и только релевантные ему метрики; отсутствующие не выдумываются | spec: История 15 → T03 (сдан) |
| R08 | «Это, понятное дело, все субъективная вещь» | done | принято: скор — предварительная оценка, решение за человеком (см. R24); аргументация обязательна. Закрыто таском 08: 1–2 предложения по-русски на ось, статус меняет только человек | spec: История 22 → T08 (сдан) |
| R09 | «Парсер должен стоить небольших денег» | done | **MVP стоит $0**: все каналы бесплатны и без ключей. Потолок $20–30/мес остаётся на условные источники и не тратится, пока пилот не показал пользу. **Состав расходов пересмотрен решением D43 (2026-09-08):** основной маршрут к Reddit и X — сторонний поставщик данных с оплатой за объём, официальные каналы (X API ~$0.005 за пост, Reddit OAuth) — запасной путь. Конкретные расценки берутся ресёрчем таска 17 на дату внедрения, а не из этой строки. LLM — через подписки Claude/Codex, ноль сверх подписки. Протокол резерва/расхода сдан таском 05 | spec: Истории 36–41, §Бюджет → T05 (сдан) |
| R10 | «поиск некоторых идей по всему интернету» | done | **широкий охват — это композиция разрешённых каналов и независимых классов сигналов, а не список из восьми площадок.** Приёмка: не меньше трёх классов сигналов (проблема / запуск / внедрение / внимание / деньги / финансирование) из нескольких независимых семейств каналов (официальные API, RSS/Atom, открытые датасеты). MVP: Hacker News, Product Hunt Atom, GitHub, Stack Exchange, курируемые RSS. Восемь исходных площадок — снятое основание, см. D01. **Закрыто 2026-09-07 слиянием T09 и T10:** ревьюер подтвердил по коду, что три класса производятся (внедрение — `stargazers_count`/`forks_count`, проблема — `open_issues_count`, внимание — `view_count`), а семейства каналов независимы (официальные API, Atom-фид, курируемые RSS). **T17 сдан 2026-09-09** (после D43–D57): маршрут `x_apify` расширяет охват сверх минимума R10, не заменяя его — три класса сигналов R10 уже подтверждены без него. Reddit в этом заходе отложен (D54), способ для discussions снят решением D21, само требование — нет | spec: Истории 4–6, §Источники → T02, T09, T10, T16, T17 (сдан) |
| R11 | «которые, в принципе, потом я могу реализовать» | done | профиль пользователя в `config/profile.md`, заполняет сам; текст идёт в промпт оценки оси «простота реализации». **Закрыто 2026-09-07 таском 08:** механизм сдан, файл создан с маркером `[ЗАПОЛНИ]`; содержимое — на пользователе (см. G06) | spec: Истории 27–28, 32 → T08 (сдан) |
| R12 | «западная идея, которая стрельнула… просто переносится практически один в один на РФ рынок, на рынок СНГ» | done | проверка «есть ли аналог в СНГ» по финалистам (топ по скору), не по всем. **Закрыто 2026-09-07 таском 11:** `dossier.select_finalists` берёт идеи выше порога `dossier.min_level` из конфига, до потолка `dossier.max_finalists_per_run`. R12.4 (постановка мимо порога) — API-поддержка сдана таском 11, кнопка и своя очередь идемпотентности — **закрыто 2026-09-08 таском 13** | spec: Истории 51–56, 59a → T11 (сдан), T13 (сдан) |
| R13 | «Показатели, как правило, в полтора-два раза ниже при хорошем исходе» | dropped | пользователь: «прикидку по рынку СНГ вообще бы не делал… Пометку по западному кейсу я бы точно оставлял» | — |
| R14 | «преимущественно какой-то небольшой несложный дашборд» | done | локальное веб-приложение: небольшой сервер + страница в браузере. **Закрыто 2026-09-07 таском 12:** `idea-scout serve` поднимает FastAPI на loopback, одна статическая страница | spec: История 58 → T12 (сдан) |
| R15 | «куда будут в принципе эти идеи складироваться» | done | локальная SQLite-база рядом с проектом. Реализация и миграции повторно проверены final code review 2026-09-11 | spec: История 7 → T01, T07 (сданы) |
| R16 | «чтобы визуальных можно было просматривать» | done | плотный список строками, как в Linear; карточка раскрывается сбоку. **Закрыто 2026-09-07 таском 12:** строки-карточки, панель сбоку по клику, листание с клавиатуры — за таском 13 (A03) | spec: Истории 58, 67 → T12 (сдан) |
| R17 | «будут создаваться некие карточки… по а каждая идея» | done | карточка на идею; раскрытие панелью, не отдельной страницей. **Закрыто 2026-09-07 таском 12** | spec: Истории 59, 59a → T12 (сдан) |
| R18 | «со ссылкой на первый источник чтобы если идея интересно я более подробно в это погрузился» | done | обязательное поле «ссылка на первоисточник» у каждой карточки. **Закрыто 2026-09-07 таском 12:** `primary_source_url` в превью, `sources[0]` в досье, только `http`/`https` (`_safe_url`) | spec: Истории 61–62 → T12 (сдан) |
| R19 | «алгоритм который будет автоматически разносить идеи по категориям» | done | две независимые раскладки: уровень A/B/C по скору + тема как тег (бот, расширение, SaaS, агентская система, API-сервис, микроинструмент, прочее); «инфопродукт» убран — противоречит выбранному целевому классу «софт». **Закрыто 2026-09-07 таском 08:** уровень считает код (`compute_level`), `level` от модели игнорируется — проверено тестом, где модель вернула чужой уровень; смена весов/порогов в `config/rubric.yml` не стоит вызова модели — проверено тестом-мутацией | spec: Истории 29–31 → T08 (сдан) |
| R20 | «в зависимости от того насколько они соответствуют тем или иным параметрам результативности эффективности качества продукта» | done | четыре оси: переносимость на РФ/СНГ, подтверждённость метрик, простота реализации, размер результата. Закрыто таском 08: рубрика различает «плохо» (низкий балл) и «нет данных» (`value=None`, не ноль) | spec: Истории 23–25 → T08 (сдан) |
| R21 | «алгоритм сам не определяет и не выкидывает идеи» | done | ничего не удаляется и не отсеивается: низкий скор = ниже в списке и за фильтром. Lifecycle и повторный сбор повторно проверены final code review 2026-09-11 | spec: Истории 7–8, 33 → T01, T07 (сданы) |
| R22 | «может делать предварительную оценку на основе тех или иных параметров» | done | цепочка провайдеров: `claude -p` → `codex exec` → пауза до сброса лимита. T06 сдал цепочку (частично — переключение работает для `research`, не для `evaluate`, см. D11), **таск 08 закрыл саму оценку**: `score()` — один вызов `mode="evaluate"`, исход, отличный от `ok`, не проглатывается | spec: Истории 23, 42–45, 49–50 → T06 (сдан частично), T08 (сдан) |
| R23 | «писать свою аргументацию» | done | та же цепочка; аргументация — часть JSON-ответа модели. Закрыто таском 08: 1–2 предложения по-русски на каждую из четырёх осей | spec: История 26 → T08 (сдан) |
| R24 | «основное решение остается за человека» | done | статусы ставит человек; алгоритм только предлагает скор и аргументацию. **Закрыто 2026-09-08 таском 13:** `new`/`favorite`/`rejected` — клик и клавиша, ни один код-путь конвейера не меняет статус (проверено тестом) | spec: История 34 → T13 (сдан) |
| R25 | «в конечном счете это выбор… после того, как идея допустим выбрана» | done | выбор = статус «взята в работу», он же точка входа в генерацию проекта. Точка входа (`scaffold.create_project`) сдана таском 14. **Закрыто 2026-09-08 таском 13:** кнопка → HTTP → создание проекта → статус `in_work` только при `write_ok=True`; идемпотентность по идее, не по `op_id` | spec: История 70 → T13 (сдан), T14 (сдан) |
| R26 | «можно будет на основе нее сразу перейти в рабочее поле, в рабочий проект в VS Code» | done | соседняя папка внутри фиксированной директории из конфига + автоматический `code <path>`. **Закрыто 2026-09-07 таском 14:** `create_dir`+`open_editor`, `code` через `shutil.which`, `subprocess.run(shell=False)` | spec: История 70 → T14 (сдан) |
| R27 | «по этой идее… на основе карточки проекта должны создаваться необходимые системные файлы для работы с агентами» | done | генерируются AGENTS.md, CLAUDE.md (ссылка), .gitignore, досье идеи в виде готового брифа. **Закрыто 2026-09-07 таском 14** | spec: Истории 71–74 → T14 (сдан) |
| R28 | «по агентской системе, по определенной структуре» | done | единая структура: AGENTS.md как источник, файл состояния работы, журнал решений. **Закрыто 2026-09-07 таском 14:** все файлы шаблонов сгенерированы детерминированно | spec: История 75 → T14 (сдан) |
| R29 | «структура должна учитывать возможность работы не только в кодексе и в open коде» | done | **В требовании склеены две разные вещи, и это разделено решением D49 (2026-09-08).** (1) Цепочка LLM-провайдеров внутри парсера (T06, сдан) — там ровно два CLI, `claude -p` и `codex exec`; OpenCode провайдером не делается, спека его в Историях 42–50 не называет. (2) Единая файловая структура генерируемого проекта, читаемая Claude Code, Codex и OpenCode (T14, сдан) — вот это про OpenCode, и это остаётся в силе. **Закрыто final acceptance 2026-09-11:** живая проверка второй половины проведена — реальный `scaffold.templates.files_for()` сгенерировал тестовый проект (единственный `AGENTS.md` + тонкий `CLAUDE.md` с `@AGENTS.md`), и все три CLI на этой машине прочитали и верно пересказали его содержимое: `claude -p` (модель haiku) через `@`-инклюд, `codex exec --sandbox read-only` (авто-обнаружение `AGENTS.md` в корне, без конфигурации), `opencode run --agent build` (бесплатная модель `opencode/mimo-v2.5-free`, авто-обнаружение `AGENTS.md`). Все три вызова — однократные, ограниченные, без сетевых сборов данных | spec: Истории 75–76 → T06, T14 (сданы) |
| R30 | «лимиты могут кончаться на одном агенте, чтобы можно было без проблем приходить на другой агент» | done | **Закрыто решениями D47 и D59.** Изоляция ради fallback не ослабляется: `evaluate` и `research` в V1 при лимите Claude ждут в очереди до точного `retry_at`; Codex research fallback отключён, пока CLI не даёт capability allowlist. Это принятая безопасная семантика V1, а не долг | spec: Истории 46–48, 76 → T06; D47, D59 |
| R31 | «не знаю, насколько она реализуема и сколько требует затрат в токенах» | done | оценено: автопереключение агентов внутри разработки нереализуемо (интерактивные сессии); реализуется как файловое состояние + цепочка провайдеров в самом парсере. **Закрыто 2026-09-07 таском 14:** `.state.json` версии 1 + `AGENTS.md` с разделом ручного переключения | spec: История 77 → T14 (сдан) |
| R32i | *(подразумевается)* у идеи есть состояние: новая / просмотрена / выбрана / отклонена | done | четыре статуса: новая / в избранном / отклонена / взята в работу; отклонённые не удаляются и не всплывают при повторном сборе. **Закрыто 2026-09-08 таском 13:** `in_work` недостижим обычным статусным маршрутом (422, проверено тестом) | spec: Истории 63–64 → T01 (сдан), T13 (сдан) |
| R33i | *(подразумевается)* парсер запускается регулярно, а не один раз | done | launchd-агенты: суточный сбор в заданное время + короткий повтор обработки очереди отдельным интервалом. Абсолютные пути к Python/CLI/конфигу/`.env`, без секретов в plist. **Закрыто 2026-09-08 таском 15:** заодно закрыт R33i.1 (сон догоняется, выключение — нет; сбор продолжается с курсора) и R38i.1 в CLI-канале (`doctor`/`report` дают ту же видимость исходов источников, что дашборд, для безголовых прогонов) | spec: Истории 9–10 → T15 (сдан) |
| R34i | *(подразумевается)* дедупликация — одна идея не должна дублироваться между запусками и источниками | done | дедупликация по URL, домену продукта и нечёткому совпадению названия; обратимое слияние через `merge`/`unmerge` | spec: Истории 11–13 → T04 (сдан) |
| R35i | *(подразумевается)* «подтверждённые показатели» требуют пруфа: где именно заявлена цифра | done | поле proof: тип (verified / скрин / листинг / только слова) + ссылка на подтверждение | spec: Истории 18–19 → T03 (сдан) |
| R36i | *(подразумевается)* где всё это работает — локально или на сервере | deferred | локально на маке, с заделом под перенос на VPS (не в этой сборке). **Задел подтверждён 2026-09-08 таском 15:** конвейер без macOS-зависимостей (только адаптер `install-schedule` привязан к launchd и заменяется cron/systemd при переносе), все пути из конфига — реальная работа на VPS остаётся вне рамок сборки | spec: Вне рамок |
| R37i | *(подразумевается)* доступ к источникам: какие API/ключи нужны и что делать без них | done | **MVP не требует ни одного ключа.** Ключ нужен только условным источникам: токен стороннего поставщика данных (`APIFY_TOKEN` или аналог — основной маршрут по D43), Reddit OAuth и X API (запасной путь), необязательный Stack Exchange key ради квоты. Все в .env, пользователь вписывает сам; без ключа источник даёт исход «нужна настройка» и ноль сетевых вызовов. **Закрыто 2026-09-07 слиянием T09 и T10:** `KNOWN_SECRETS` и `.env.example` несут `GITHUB_TOKEN`, `STACKEXCHANGE_KEY` (оба необязательны — без них урезанная квота, а не отказ), `TAVILY_API_KEY`, `EXA_API_KEY` и намеренно не заполняемый `BRAVE_SEARCH_API_KEY` | spec: Открытые места → T02, T16, T17 |
| R38i | *(подразумевается)* человек должен уметь искать/фильтровать/сортировать накопленные идеи | done | фильтры по уровню A/B/C, теме, уровню доверия, статусу; сортировка по скору и дате. **Закрыто 2026-09-07 таском 12:** все шесть фильтров (D37 добавил доверие) плюс `q`, сортировка по приоритету/уровню/дате (D40), серверная пагинация | spec: Истории 65–66 → T12 (сдан) |
| G01 | *(от пользователя)* «До 18 месяцев» — кейсы старше помечаются как «архив» и уходят вниз, но не удаляются | done | ответ в брифинге. **Закрыто 2026-09-07:** T07 — `archived` пишет `set_prefilter`; T12 — фильтр `archived` в дашборде, архивные по умолчанию видны только через фильтр | spec: История 14 → T07 (сдан), T12 (сдан) |
| G02 | *(от пользователя)* «если были похожие проекты в СНГ (похожая идея, аудитория, проблема и т.д.) показывать эти продукты внутри карточки как идея "параллельного досье", с ссылками на продукты + их MRR (или другой важный показатель)» | done | ответ в брифинге; при сложности — deferred, минимум = информация по кейсу-первоисточнику. **Закрыто 2026-09-07 таском 11:** `ResearchReport.analogues` несёт `name`/`url`/`similarity_note_ru` и `metric_claims` (те же `MetricClaim`, что у основного кейса, через `evidence.accept`); три состояния — не проверяли/проверили и не нашли/нашли N, ошибка инструмента не превращается в «не нашли», строки «ниша свободна» нет нигде (проверено грепом в тесте) | spec: История 55 → T11 (сдан) |
| G03 | *(от пользователя)* «система умела только заранее разрешённый набор действий… Важно не делать универсальный shell-executor и не позволять дашборду/LLM выполнять произвольные команды. Нужно ограничить пути только рабочей директорией проекта, запретить ../, выход за неё, удаление/перемещение файлов вне неё и перезапись существующих папок без подтверждения» | done | ответ в брифинге. **Закрыто 2026-09-07:** T06 — изоляция CLI-вызовов модели (сдан); T14 — три операции и ни одной больше (`create_dir`/`write_new_file`/`open_editor`), `safe_path` отвергает пустую строку/абсолютный путь/`..`/симлинк наружу, существующая папка не перезаписывается. G03.1 (дашборд не даёт произвольного стороннего доступа к изменяющим маршрутам) — **закрыто 2026-09-08 таском 13:** локальный непредсказуемый токен, Host/Origin, GET не меняет состояние, CORS не разрешён | spec: Истории 43–45, 69, 78–80 → T06 (сдан), T14 (сдан), T13 (сдан) **Уточнено D50 (2026-09-08):** «минимальное окружение» включает `USER` — без него CLI не находит учётные данные; список остаётся закрытым и закреплён тестом |
| G04 | *(от пользователя)* «Основную информацию… нужно выносить в превью карточки… должна быть возможность провалиться внутрь карточки и изучить более подробное досье» | done | ответ в брифинге; состав превью — на мой выбор, без переизбытка. **Закрыто 2026-09-07 таском 12:** превью — название/суть/тема/метрика/уровень/рынок/возраст/блокеры, досье по клику панелью сбоку | spec: Истории 59–60 → T12 (сдан) |
| G05 | *(от пользователя)* «более сложные системы (проекты) также можно не откидывать, просто складывать их в отдельное место в дашборде. К ним потом можно будет вернуться позже» | done | ответ в брифинге. **T08 сдан:** ничего не отбрасывается, низкий уровень не удаляется и не прячется навсегда. **Закрыто 2026-09-07 таском 12:** фильтр по уровню/теме/`scope`, тяжёлые (`scope: team`) по умолчанию скрыты, но доступны фильтром — не удалены, а именно «отдельное место» | spec: История 2 → T08 (сдан), T12 (сдан) |
| G06 | *(от пользователя)* «Файл профиля, я его заполню» — профиль влияет на ось «простота реализации» | done | ответ в брифинге. **Механизм сдан таском 08** 2026-09-07: пока маркер `[ЗАПОЛНИ]` на месте, ось `simplicity` = `None` с `unknown_reason="профиль не заполнен"`, оценка не пропадает. **Закрыто 2026-09-08:** пользователь заполнил `config/profile.md` сам (916 символов: доступное время, навыки и рабочий процесс, ограничения). Оркестратор проверил чтение, файл не правил: `handlers/score.py::_profile_path` находит его рядом с конфигом, маркера в тексте нет, `profile_hash` считается — значит ось `simplicity` теперь оценивается моделью, а не гасится | spec: Открытые места → T08 (сдан) |
| G07 | *(от пользователя)* «Русский, оригинал рядом» — интерфейс и аргументация по-русски, названия/цитаты/ссылки в оригинале | done | ответ в брифинге. **Закрыто 2026-09-07 таском 12:** интерфейс дашборда по-русски, названия/цитаты/ссылки не переводятся | spec: История 68 → T12 (сдан) |
| G08 | *(от пользователя)* «10-20 в неделю» — столько идей должно доходить до разбора | done | ответ в брифинге. **T08 сдан:** `store.unreviewed_top(n)` — единственная реализация порции, `N` в конфиге (`scoring.unreviewed_daily_n = 3`, по умолчанию 3/день). **Закрыто 2026-09-08 таском 13:** `GET /api/ideas?review=top` показывает верхние N первыми, остальные — обычным фильтром | spec: История 35 → T08 (сдан), T13 (сдан) |
| G09 | *(от пользователя)* «По TrustMRR выбираю 1 вариант, чтобы не блокировать запуск реализации» — написать за письменным разрешением; до ответа источник выключен | done | условия TrustMRR §9.3 запрещают и перенос найденных бизнесов, и подачу данных в оценочную систему без письменного разрешения. **Закрыто для MVP решением D46 (пользователь, 2026-09-08):** запрос не отправляется, источник остаётся выключенным до отдельного письменного разрешения — если оно вообще будет запрошено | spec: §Источники → T02 (сдан: §9.3 процитирован, статус «выключен», адаптера нет) |

## Решения по ходу сборки

Строка сюда попадает, только когда пользователь письменно меняет ранее данное
слово или когда сборка доказала, что план неверен. **Требование при этом не
исчезает — меняется основание, то есть способ его выполнить.**

| ID | Что изменилось | Чьё решение и когда | Почему |
|----|----------------|---------------------|--------|
| D59 | В V1 Codex отключается как `research` fallback: `--sandbox read-only` не гарантирует allowlist только WebSearch/WebFetch и оставляет shell/file read. До появления гарантируемого capability allowlist research-chain использует только Claude; при лимите работа ждёт до `retry_after` | пользователь, 2026-09-10, final code review | Подтверждено BE-02: текущая изоляция нарушает G03/R22.3–R22.4. Это contract fix V1, не новая product logic |
| D60 | `ProjectRecord` и таблица `projects` получают сохраняемое `error: str | None`; новая backward-compatible миграция добавляет nullable-колонку, а повтор/reload восстанавливает исходную partial-project diagnostic | пользователь, 2026-09-10, final code review | Подтверждено WEB-08/BE-11: прежняя схема не могла выполнить уже записанный контракт идемпотентности |
| D61 | `Store.complete_job` backward-compatibly дополняется `retry_at: datetime | None = None`: для `pending` сообщённое provider-время сохраняется точно, иначе остаётся локальный exponential backoff | пользователь, 2026-09-10, final code review | Подтверждено BE-04: `RetryJob.retry_at` терялся между queue и store, нарушая R30.1/R30.2 |
| D62 | `Store` получает точную проекцию `signals_for_period(idea_id, period)`, которая join-ом привязывает `SignalObservation.raw_item_id` к `raw_items.fetched_at`; period report больше не восстанавливает связь по одному `source` | пользователь, 2026-09-10, final code review | Подтверждено BE-10/SECOPS-05: старый сигнал того же source мог попасть в новый месяц |
| D01 | R10.1 снят: «приоритет доступа: бесплатный API → готовый scraper/Apify → собственный HTML-парсер». Ступеней Apify и собственного HTML-парсинга в активном плане больше нет | пользователь, 2026-09-06: «подтвреждаю, сделай все, что нужно, до волны 2» — к разбору исследования, где это названо как требующее его решения | Ступени писались ради площадок, чьи условия запрещают сам сбор: Indie Hackers (ToS §(h)), Acquire.com («any robot, spider or other automatic device»), Flippa («express written consent»), X («scraping… expressly prohibited»). Apify этого не меняет — это тот же сбор чужими руками, и его условия оставляют разрешения на пользователе |
| D02 | R10.2 снят: «HTML-парсеры модульными, чтобы правились селекторы, а не код». `config/selectors.yml` в этой сборке не создаётся | там же | Требование про удобство сопровождения HTML-парсеров, которых больше нет. Разбор структуры трёх витрин сохранён в паспортах таска 02 как задел на случай письменного разрешения; если оно придёт, требование возвращается вместе с адаптером |
| D03 | Основание R10 переписано с «восьми названных площадок» на «классы сигналов из разрешённых семейств каналов» | там же | Дословные слова требования — «поиск некоторых идей по всему интернету». Восемь площадок были моим описанием способа, а не словами пользователя, поэтому способ меняется без снятия требования |
| D04 | Добавлено: search-провайдер не может быть обычным `Source` | оркестратор, 2026-09-06 | История 7 («найденное записывается на диск до фильтра») и условия Brave/Tavily/Exa несовместимы: их планы запрещают хранение результатов. Нужна вторая форма — транзитная находка с политикой, иначе контракт заставляет нарушить договор в момент сохранения |
| D05 | Контракт `evidence` исправлен: `extract` принимает три аргумента; `mentions` — это `list[tuple[int, ProductMention]]`; у `SignalObservation` вместо `idea_id` стоит `mention_id`; зона таска 03 в `store.py` — шесть методов, а не два | оркестратор, 2026-09-06, по возврату `BLOCKED` от исполнителя таска 03 | Сборка доказала, что план неверен: `ProductMention` не несёт своего `id`, а наблюдение сигнала физически случается раньше, чем `upsert_idea` заводит идею. Исполнитель остановился до первой строки кода и не стал выбирать за оркестратора — это правильное поведение, а не отказ работать |
| D06 | Контракт `evidence.accept` исправлен: `accept(claims: list[tuple[int, MetricClaim]], idea_id: int) -> list[AcceptedMetric]`. Записан порядок вызова в конвейере и закреплено, что правила доверия принимает `evidence`, а не `store` | оркестратор, 2026-09-06, по второму возврату `BLOCKED` от исполнителя таска 03 | Тот же дефект, что D05, в соседнем месте: `AcceptedMetric` требует `idea_id` и `claim_id`, а из голого списка `MetricClaim` не выводится ни один. `claim_id` выдаёт `save_claims`, `idea_id` — `upsert_idea` из таска 04. Исполнитель отказался выдумать контракт и оставил правила доверия на неверной границе, назвав это прямо, — правильное поведение |
| D07 | Введено общее правило: ключи конфига расширяются дописыванием, объявляются здесь с владельцем — как схема. Потолок обращений к модели передаётся `extract` именованным аргументом со значением по умолчанию, значение даёт конвейер T07. Проверка связи «принятый факт ↔ идея» закреплена за `store`, правила доверия — за `evidence` | оркестратор, 2026-09-06, по третьему возврату `BLOCKED` от исполнителя таска 03 | Потолку обращений к модели негде было лежать: в `Config` поля нет, менять сигнатуру `extract` запрещено, `LLMProvider` понятия бюджета не несёт. Тупик касался не только таска 03 — свой ключ конфига понадобится ещё T05, T10 и T15, поэтому решено правилом, а не заплаткой |
| D08 | Контракт `identity` (T04) дополнен до первой строки кода: добавлены типы `IdeaRef`, `Candidate`, `ProductIdentity`; добавлен метод `Store.known_identities()` (владелец T04, без миграции); `resolve` теперь чистая функция, принимающая данные сравнения аргументом, а не читающая базу сама; `merge`/`unmerge` — тонкие обёртки над одноимёнными методами `store` | оркестратор, 2026-09-06, проверка стыков перед волной 2 | Таблица границ модулей называла `resolve(m) -> IdeaRef \| Candidate`, но ни один тип не был определён, а `identity` не имеет своего входа в базу — тот же класс дефекта, что D05: тип назван в границе, а не в контракте. Найдено чтением, до BLOCKED, по прямому указанию проверить этот стык заранее |
| D09 | Сигнатура `identity.resolve` исправлена: `resolve(item: RawItem, mention: ProductMention, known: list[ProductIdentity]) -> IdeaRef \| Candidate` — добавлен параметр `item` | оркестратор, 2026-09-06, по возврату `BLOCKED` от исполнителя таска 04 (четвёртый круг, первый по существу) | «Устойчивый ID источника» из критерия приёмки — это `source`/`source_item_id`, поля `RawItem`, а не `ProductMention`; D08 этого не учёл. Тот же класс дефекта, что D05/D06 в таске 03: параметр, нужный функции по смыслу, не был включён в сигнатуру. Исполнитель отказался угадывать и не потратил ни одной правки кода — файлы не менял, коммит не делал |
| D10 | Сигнатура `identity.mentions` исправлена: `mentions(raw_id: int, item: RawItem) -> list[ProductMention]` — добавлен параметр `raw_id` | оркестратор, 2026-09-06, по возврату `BLOCKED` от исполнителя таска 04 (шестой круг) | `ProductMention.raw_item_id` обязателен, а `RawItem` своего id не несёт — он появляется только из `store.save_raw`. Тот же класс дефекта, что D05/D06/D09: конвейер обязан звать `save_raw` раньше `mentions` и передавать `raw_id` явно. Исполнитель снова не потратил ни одной правки кода — 140 тестов и чистый ruff подтверждают, что дерево осталось нетронутым |
| D11 | Признано: R29/R30 для режима `evaluate` (скоринг идей) не имеет переключения между провайдерами — только Claude умеет `evaluate`, лимит Claude оставляет работу в очереди без обращения к Codex. Для режима `research` (досье, аналоги) переключение работает как задумано | оркестратор, 2026-09-06, по находке ревьюера манифест+спека на таске 06 | Таск 06 сам решил это явно, до кода: `--sandbox read-only` у Codex — политика для shell-команд, а не отсутствие инструментов, и адаптер обязан вернуть `unsupported_mode`, а не выполнить оценку с более широкими правами, чем разрешено. Ослабление изоляции ради фолбэка — решение про G03 (запрет произвольного выполнения), а не баг кода; без письменного решения пользователя единственный безопасный выбор — не расширять полномочия. R29/R30 остаются частично выполненными до этого решения, ограничение задокументировано, а не скрыто |
| D12 | Добавлен метод `Store.set_prefilter(idea_id, *, priority, not_scored_reason, archived, tx)` — владелец T07, без миграции | оркестратор, 2026-09-06, preflight-проверка стыков перед волной 3 | Колонки `ideas.priority`, `ideas.not_scored_reason`, `ideas.archived` созданы миграцией `001_init.sql`, но за всю сборку ни разу не записывались: в API хранения есть только читатели (`list_ideas`, `get_idea`, `unreviewed_top`). Префильтр живёт в `pipeline`, а `pipeline` SQL не пишет — то есть R21.1 и G01 были невыполнимы как записаны. Тот же класс дефекта, что D05 и D08: обязанность в критерии приёмки есть, канала исполнения нет. Найдено чтением, до первой строки кода |
| D13 | `build_context` заполняет `AppContext.llm_chain` через `chain_from_config`; это единственная разрешённая T07 правка `cli.py`, запрет ручной регистрации команд остаётся | оркестратор, 2026-09-06, preflight-проверка стыков перед волной 3 | `build_context` оставлял `llm_chain=None`, а обработчик по контракту «ничего не конструирует сам». Обработчик `reprocess_raw` (T07) запускается командой `work` (T05, сдана, чужая зона), которая цепочку не заполняет, — путь восстановления не мог позвать `evidence.extract`, и починить это внутри своей зоны T07 не мог. То же ждало T08 и T11. `chain_from_config` создаёт только объекты провайдеров: ни сети, ни процессов до `complete()` |
| D14 | Добавлен метод `Store.attach_mention_to_idea(mention_id, idea_id, link_reason, tx) -> int` — владелец T07, без миграции. Закреплены его правила: к поглощённой идее не привязывает, повторная привязка к той же идее идемпотентна, переброс упоминания на другую идею запрещён (это слияние, а слияние — отдельная осознанная операция) | оркестратор, 2026-09-06, по возврату `BLOCKED` от исполнителя таска 07 | Ветка `IdeaRef` из решения D08 — «уверенное совпадение, авто-присоединение к существующей идее» — была объявлена типом, но канала исполнения не получила: `upsert_idea` у свежего упоминания (`product_mentions.idea_id IS NULL`) всегда заводит НОВУЮ идею, а `link_raw_to_idea` связывает публикацию и оставляет `product_mentions.idea_id` пустым. Последствие проверяемо по коду: `save_accepted` требует `pm.idea_id = ? AND irl.idea_id = ?` (решение D06), поэтому каждый принятый факт по дедуплицированному продукту падал бы `ValueError`, а `claims_for` возвращал бы пусто. Четвёртый дефект того же класса, что D05, D08 и D12. Исполнитель остановился до первой строки кода и не стал чинить контракт сам — правильное поведение |
| D15 | Санкционированы задним числом четыре новые поверхности `Store` таска 07 (`unprocessed_raw_count`, `raw_for_reprocess`, `replace_evidence_for_raw`, `case_dates_for_idea`); `last_raw_saved_count` отклонён. Область удаляющего пути ограничена контрактом. Уточнено правило «Возраст кейса»: возраст идеи — свежайшая из дат всех связанных с ней публикаций и принятых фактов, поэлементное правило датирует один элемент | оркестратор, 2026-09-06, по второму кругу ревью таска 07 | Пять поверхностей добавлены исполнителем молча — по правилу зоны это был `BLOCKED`. Четыре из них понадобились ровно для ремонта, который потребовал оркестратор (замена фактов вместо дублей, честное «сохранено», адресное восстановление, возраст по связанным кейсам), поэтому сохраняются с записью в контракт. `last_raw_saved_count` отклонён по существу: изменяемое состояние на общем объекте — не канал возврата значения, два вызывающих затрут его молча. `replace_evidence_for_raw` — единственный удаляющий путь в сборке, поэтому его границы записаны контрактом, а не оставлены реализации: идей, статусов, оценок и связей он не касается ни при каких условиях |
| D16 | `Store.save_raw` возвращает `SaveRawResult(raw_ids, written_count)` вместо `list[int]`; тип живёт в `types.py`. Записано общее правило: значение возвращается возвратом, скрытые каналы (изменяемое состояние на общем объекте, подкласс со скрытым полем, чтение через `getattr` с умолчанием) каналом возврата не являются | оркестратор, 2026-09-06, по названной исполнителем поправке в отчёте четвёртого круга таска 07 | Критерий приёмки требует различать «собрано» и «сохранено», а `list[int]` число записанных донести не мог: неизменившийся элемент перечитывается каждым прогоном и записью не является. Два обхода отклонены — изменяемый атрибут на `Store` решением D15, подкласс `list` со скрытым полем здесь: объявленный тип поля не несёт, а `getattr` с умолчанием превращал бы будущую поломку контракта в тихо неверное «сохранено», равное «собрано». Единственная смена сигнатуры сданного таска за сборку; на `list[int]` не опирался никто, кроме конвейера T07 |
| D17 | `discover()` в `sources/__init__.py` дополнен: модуль может объявить множественную регистрацию `SOURCES: dict[str, SourceFactory]` вместо одиночной `SOURCE`, каждая фабрика внутри подчиняется тому же правилу — статичная `.policy`, без конструирования | оркестратор, 2026-09-06, точечный preflight перед волной 4 | Курируемые RSS (T09) — один файл на N источников: по записи в `config/feeds.yml` формируется свой `SourcePolicy` с собственным `source = "rss:<id>"`, взятым из паспорта T16 дословно. Прежний `discover()` мог прочитать ровно одну статичную политику с одного модуля и физически не мог выразить N политик, которых заранее не знает и не должен знать. Тот же класс дефекта, что D08/D12/D14: обязанность («добавление фида не требует правки Python») записана в тикете, а канала исполнения не было. Найдено чтением контракта до кода |
| D18 | Типизирован контракт границы `discovery`: `class DiscoveryProvider(Protocol): def search(self, query: str, budget: Reservation) -> DiscoveryResult`, `def policy_gate(hit: DiscoveryHit, policy: SourcePolicy | None) -> PolicyDecision` | оркестратор, 2026-09-06, точечный preflight перед волной 4 | Тот же класс дефекта, что D08: имя функции названо в таблице границ модулей, но без единого типа параметра — `budget` и `policy` не имели типов вовсе. `budget: Reservation`, потому что резерв создаётся ДО вызова провайдера (порядок из тикета 10) и передаётся готовым, тем же приёмом, каким `local_attempt_id` резервации существует до внешнего вызова. `policy: SourcePolicy | None` — `None` валиден и означает «неизвестная политика», согласно критерию приёмки тикета; откуда вызывающий берёт известную политику для конкретной находки — решение T10, контрактом не предписывается |
| D19 | `DiscoveryResult` дополнен полем `retry_after: datetime | None` — «только если провайдер сообщил, иначе `None`», теми же словами и с тем же смыслом, что одноимённое поле `Completion` у T06 | оркестратор, 2026-09-06, ревью первого круга таска 10 | Паспорт `docs/sources/search-provider.md`, таблица «Поведение при недоступности», предписывает дословно: «429 — `rate_limited`, `retry_after` из заголовка провайдера, иначе `None`». Выразить это `DiscoveryResult` не мог: полей ровно шесть, и ни одно не подходит — `warnings` это текст для человека, а не машинно читаемое время. Тот же класс дефекта, что D08/D12/D14/D17/D18: обязанность записана (здесь — в паспорте, а не в тикете), канала исполнения нет. Исполнитель первого круга это не поднял, а молча выбросил `retry_after` — и это отдельная находка ревью, а не оправдание |
| D20 | Записано, на какой вопрос отвечает `policy_gate`: можно ли взять документ издателя разрешённым каналом и сохранить его. `commercial_status` в воротах не участвует вовсе; `retention_days == 0` и `content_mode == "transient"` дают `refused`; `metadata_excerpt` и `snapshot` дают `allowed`. `allowed` — право на разрешённый канал издателя, а не на полную загрузку страницы, и возвращённые этим каналом записи проверяются против политики до сохранения. Недоступный канал даёт `url_only` с предупреждением, не тишину | оркестратор, 2026-09-06, ревью первого круга таска 10 | Два ревьюера прочитали ворота противоположно: один потребовал, чтобы `commercial_status="unknown"` давал не больше `url_only`, другой показал, что правило про `commercial_status` выдумано исполнителем и отказывает Stack Exchange и Product Hunt Atom — обоим в составе MVP. Расхождение двух добросовестных прочтений и есть доказательство, что контракт не сказал главного. Проверено по паспортам: `snapshot` не стоит НИ У ОДНОГО источника сборки, `metadata_excerpt` — у семи, `unknown` — у большинства. Значит любое правило, выводящее вердикт из `commercial_status`, либо выключает почти всё, либо не выключает ничего |
| D21 | Критерий тикета 09 «GitHub: search по репозиториям, issues и discussions» разделён: `search/repositories` добавлен в паспорт и реализуется; **discussions снят из плана волны 4** — в REST-поиске их нет, GitHub отдаёт их только через GraphQL, то есть это другой канал с другой аутентификацией и другими условиями, а паспорта под него нет | оркестратор, 2026-09-06, ревью первого круга таска 09 | Правило «источник без паспорта не реализуется» исключений не имеет, и правильным ходом исполнителя был `BLOCKED`, а не тихое сужение критерия до выполнимой трети — это отдельная находка ревью. Половина, которую можно закрыть паспортом, закрыта: `search/repositories` — тот же официальный Search API, тот же потолок 1000 и тот же счётчик 30 запросов в минуту, что у `search/issues`, снято живьём с docs.github.com. **Требование R10 не снимается — снимается способ**, ровно как в D01–D03, где условия площадок отменили маршрут, но не требование охвата. Канал вернётся отдельным паспортом, если понадобится. Снятие способа — решение оркестратора; снять само требование может только пользователь |

| D22 | `Store.save_discovery_run` принимает `spend_usd: Decimal \| None`; неизвестный расход — `NULL`. Колонка `spend_known` из `005_discovery.sql` убрана: признак «расход неизвестен» выражается одним `spend_usd IS NULL` | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 | Контракт объявил `spend_usd: Decimal`, а протокол бюджета (`mark_unknown`, таск 05) прямо предусматривает исход «резерв был, ответ потерян, сколько списали — неизвестно». Реализация разошлась с контрактом правильно по смыслу и неправильно по букве: тип обязан быть расширен решением, а не разойтись молча. Второй признак того же факта — способ, которым два поля расходятся: комментарий в самой миграции говорит, что `spend_known` дублирует `spend_usd IS NULL`, а сторожа, который держал бы их согласованными, нет |
| D23 | Параметр `config` в `run_discovery` и `run_discovery_batch` перестаёт быть необязательным. Умолчания живут в самом `Config`; вызывающий, которому нужны умолчания, пишет `Config()` явно | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 (находка обоих ревьюеров независимо) | `config=None` означал одновременно две несовместимые вещи: «возьми контрактные умолчания потолков» и «выключателя, который надо спросить, нет». Реализация выбрала обе сразу — синтезировала `Config(discovery_provider=provider.name)`, то есть на этой ветке выключатель `discovery.provider = null` не срабатывал никогда, а проверка `_disabled_by_config` становилась мёртвой. Виноват контракт: я объявил умолчания для `config=None`, не сказав, что делает выключатель, когда конфига нет. Двойной смысл — источник целого класса «страж написан, но не сторожит»; он снимается не правилом, а тем, что необязательного параметра больше нет. В проде вызывающих у `run_discovery` пока ноль, цена решения — только правка тестов |
| D24 | `DiscoveryProvider` получает статичный атрибут `policy: SourcePolicy`, как и `Source`. Провайдер без паспорта не регистрируется и не зовётся | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 | Правило «паспорт до адаптера» исключений не имеет, но протокол `DiscoveryProvider` (D18) паспорта не требовал — я типизировал только `search`. Реализация добралась до паспорта через `getattr(provider, "policy", None)` с умолчанием, и провайдер без паспорта проходил как разрешённый. Это ровно тот скрытый канал, который запрещён решением D16: чтение через `getattr` с умолчанием превращает будущую поломку контракта в тихо неверное «разрешено». Обязанность была записана в AGENTS.md, канала исполнения в контракте не было |
| D25 | Ключ `discovery.freshness_days` снят из таблицы конфига. Окно свежести в этой сборке не применяется | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 | Ключ объявил я, опираясь на смысл («документ старше почти наверняка уже пришёл разрешённым каналом»), а не на паспорт: имя параметра свежести у Tavily паспортом `search-provider.md` не подтверждено, и исполнитель правильно отказался его выдумывать. Получилась настройка, которая ничего не делает и вешает предупреждение на каждый успешный прогон — постоянный шум на удачном пути хуже отсутствующей настройки, потому что учит не читать предупреждения. Ключ вернётся, когда паспорт назовёт параметр дословно: сначала цитата, потом настройка. Требование не затронуто — снят способ |
| D26 | `DiscoveryOutcome` дополнен значением `"not_run"` — запрос не запускался, потому что порция уперлась в потолок `discovery.max_queries_per_run` | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 | Хвост порции получал `budget_exhausted` — тот самый исход, за которым паспорт закрепил другое действие: «провайдер выключается до следующего периода». Оператор, увидевший `budget_exhausted`, ждёт нового периода; незапущенный хвост можно пустить следующей порцией прямо сейчас. Один исход на два разных действия — это потеря различия, а не экономия имени. В миграциях `DiscoveryOutcome` не участвует, цена добавления — одна строка `Literal` |
| D27 | Строка `url_only`: лицензия из известного паспорта доходит до записи; `attribution` остаётся `None`, и контракт называет причину — сохранённого содержимого нет, атрибутировать нечего, адрес сам является отсылкой. При неизвестном паспорте (`policy is None`) оба поля `None` | оркестратор, 2026-09-07, ревью второго круга ремонта таска 10 | Правило 3 («атрибуция переживает всё») сформулировано безусловно, и реализация, жёстко ставившая `license=None` даже при известном паспорте с `attribution_required`, читается как его нарушение. Но правило написано про путь содержимого — нормализацию, дедупликацию, слияние, экспорт, — а в строке `url_only` содержимого нет по построению: заголовок и сниппет находки транзитны и не сохраняются нигде (правило 1). Лицензию мы при этом знаем из паспорта, и она не стоит ничего — значит теряться она не имеет права. Разница между «нечего атрибутировать» и «атрибуцию потеряли» должна быть записана, а не выводиться каждым читающим заново |
| D28 | `FetchResult` дополнен полем `retry_after: datetime \| None` — «только если площадка сообщила, иначе `None`», теми же словами и с тем же смыслом, что у `DiscoveryResult` (D19) и `Completion` (T06) | оркестратор, 2026-09-07, ревью круга ремонта 1 таска 09 | Три паспорта предписывают брать время повтора из ответа дословно: GitHub — «`retry_after` заполняется только из заголовка `retry-after` или из `x-ratelimit-reset`», Stack Exchange — из поля `backoff`, курируемые RSS — из `Retry-After`. Выразить это `FetchResult` не мог: время доезжало текстом внутри предупреждения, то есть в поле для человека, а не для планировщика. Тот же класс дефекта, что D19, и та же причина, по которой он не был закрыт вместе с ним: `FetchResult` конструируется в двенадцати местах пяти файлов, и трогать его стоило одним ходом вместе с ремонтом, а не отдельной правкой между кругами. Круг ремонта 2 таска 09 — этот ход |
| D29 | Курсор над **окном фиксированного размера, которое читается целиком каждый прогон** (Product Hunt Atom), не фильтрует выдачу: все записи окна уходят в `save_raw`, и что из них ново, решает он. Курсор хранит свежайший `published` и служит отчётом, а не отсевом. Основание отсева — только `published`; смешивать `published` и `updated` в одном скаляре запрещено | оркестратор, 2026-09-07, ревью круга ремонта 1 таска 09 | Реализация фильтровала по `updated or published`, а курсор хранила как максимум того же выражения. Отсюда следует потеря: пост, опубликованный в 22:00 и ни разу не правленый, отбрасывается курсором 22:16, взятым с чужой записи, обновлённой в ту же минуту. Обратный выбор — фильтровать по `published` — теряет другое: правка описания старого поста никогда не доедет, а паспорт держит повторное чтение каждые 6 часов именно ради неё. Оба выбора теряют, потому что теряет сама конструкция: две разные оси времени, сжатые в один скаляр, дают ровно тот дефект, который эта сборка ловит с таска 07, — «позиция курсора оказывается выше непрочитанных данных». Третий выход есть и он дешёвый: окно 50 записей, запрос **побайтово тот же** при любом параметре (проверено паспортом), значит отсев не экономит ни одного обращения. `save_raw` уже умеет отличать вставку от обновления (D16), а `content_hash` уже меняется у поправленной записи |
| D30 | Четыре паспорта поправлены по коду круга ремонта 1: строка «`raw_payload` — элемент/XML целиком» заменена белым списком полей у Product Hunt Atom, GitHub, Stack Exchange и курируемых RSS. У GitHub дополнительно закреплены третье состояние атрибуции («лицензия не добрана») и приставки `issue:`/`repo:` в `source_item_id` | оркестратор, 2026-09-07, ревью круга ремонта 1 таска 09 | Строка про «элемент целиком» противоречила разделу «Хранение» тех же паспортов и была прямой причиной критической находки первого круга: исполнитель написал `raw_payload=dict(hit)`, и это ровно то, что паспорт в этом месте разрешал. Пока противоречие живо, следующий исполнитель повторит его добросовестно. Третье состояние атрибуции и приставки идентификатора — отступления обоснованные (незамерзающий дайджест; последовательности id у issues и репозиториев пересекаются), и потому обязаны стоять в паспорте решением, а не оставаться расхождением, которое каждый читающий разрешает заново |
| D31 | Признано долгом: `score` и `answer_count` (Stack Exchange), `comments` и `reactions` (GitHub) лежат в `raw_payload`, но ключей в `_SIGNAL_KEYS` (зона T03, сдан) не имеют — обещанные паспортами сигналы с этих полей не производятся. Требование R10 не затронуто | оркестратор, 2026-09-07, ревью круга ремонта 1 таска 09 | Приёмка R10 требует не меньше трёх классов сигналов из независимых семейств каналов, и ревьюер подтвердил по коду, что три производятся: внедрение (`stargazers_count`, `forks_count`), проблема (`open_issues_count`), внимание (`view_count`). Недостающие четыре ключа — расширение, а не блокер. Правка живёт в зоне сданного таска 03, и лезть в неё посреди волны — ровно тот способ, которым таски 03 и 16 столкнулись на фикстурах. Долг закрывается оркестратором после слияния волны 4, одним ходом и с прогоном тестов таска 03 |
| D32 | Правило закрытия резерва уточнено до трёх состояний вместо двух. `settle(actual)` — провайдер отработал и стоимость известна. `settle(0)` — **доказано, что работы не было**; сегодня этому отвечает единственный случай: запрос не покидал процесс (нет ключа, выключено настройкой, упёрлись в потолок). `mark_unknown` — всё остальное, включая 429, 401/403, 5xx, перенаправление и неразбираемое тело | оркестратор, 2026-09-07, ревью круга ремонта 3 таска 10, с живой выборкой документации Tavily | D22 сформулирован через «был ли сделан вызов», и на 429 это даёт неверный ответ: вызов был, а работы не было. Правильный признак — **известно ли, что провайдер сделал**, и состояний у него три, а не два. Дальше нужен факт, а не рассуждение, и он снят живьём: Tavily документирует «You never get charged if a URL extraction fails» для `extract` и «You never get charged if a map request fails» для `map` — **и не пишет того же про `search`**. Поставщик, назвавший «не тарифицируется при сбое» для двух маршрутов из трёх, этим сказал о третьем больше, чем молчание. Страница про лимиты цитирует 429 с заголовком `retry-after` дословно и о кредитах не говорит вовсе. Значит цитаты, разрешающей `settle(0)` на отказе, нет — а правило «сначала цитата, потом настройка» (D25) действует и здесь. Возражение против безопасного выбора — что `unknown` блокирует следующий запуск до ручной сверки — снимается арифметикой: наши потолки 10 за запуск и 30 в сутки лежат на два порядка ниже лимита площадки в 100 запросов в минуту, то есть 429 у нас означает не рутину, а поломку, и остановка ради сверки — правильная реакция, а не помеха |
| D33 | Провайдер без статичного `policy` (D24) останавливается **названной** ошибкой построения, а не необработанным `AttributeError` от обращения к атрибуту; тест стоит на названном типе | оркестратор, 2026-09-07, ревью круга ремонта 3 таска 10 (находка обоих ревьюеров независимо) | Отказ до сети есть и работает — ноль вызовов подтверждён прогоном. Но выражен он случайностью: тем, что доступ к несуществующему атрибуту бросает. Тест, закрепляющий `AttributeError`, тем самым закрепляет реализацию, а не правило: аккуратная реализация, отказывающая явно, сделала бы его красным. Провайдер без паспорта — ошибка сборки, а не состояние эксплуатации, поэтому исход `unavailable` здесь неуместен: он превратил бы неправильно собранный провайдер в штатную ситуацию, которую видно только в предупреждениях |
| D34 | Обязательных элементов атрибуции Stack Exchange — **три**, а не четыре: имя автора, ссылка на пост, название и версия лицензии. Ссылка на профиль несётся, когда площадка её даёт, и опускается, когда не даёт; её отсутствие атрибуцию не отменяет. Отсутствие любого из трёх обязательных даёт `attribution = None`, и запись до экспорта не доходит | оркестратор, 2026-09-07, живой прогон после круга ремонта 2 таска 09 | Паспорт писал «четыре обязательных элемента, и ни один из них не необязателен», исполнитель следовал этому дословно, офлайн-набор из 286 тестов был зелёным — и живой прогон уронил тест. У **удалённого или анонимного автора профиля не существует**: `owner.link` приходит `None`, ссылаться не на что, а запись теряла атрибуцию **целиком**, вместе с именем автора, ссылкой на пост и лицензией, которые все были на месте. Это и есть нарушение, которое паспорт запрещает своей же строкой «атрибуция, потерянная по дороге, — это нарушение»: три известных элемента выбрасывались из-за четвёртого, которого у площадки нет. Тот же класс пробела, что D30: паспорт описывал случай, которого не проверял живьём. Частота — одна запись из 289, то есть фикстурами этот случай не ловится вовсе, и поймать его мог только живой прогон. Заглушки остаются запрещены: неполнота машинно различима счётом элементов |
| D35 | В таблице «Поведение при недоступности» паспорта Stack Exchange разделены два случая, склеенные в одну строку: **пустой** `items` (конверт есть, список пуст) остаётся `no_new` с `complete=True`; **отсутствующий** `items` (конверта нет — испорченный фильтр) даёт `partial`, поток остаётся недочитанным, повтор без прогресса эскалирует до `error` | оркестратор, 2026-09-07, финальное ревью таска 09 | Строка «`items` пуст или отсутствует → `no_new`, `complete=True`» соединяла события с противоположным смыслом. Пустой список — ответ площадки по существу: нового нет, окно прочитано, курсор имеет право двигаться. Отсутствующий конверт — ловушка испорченного фильтра, названная этим же паспортом абзацем выше: элементов нет **по нашей вине**, и объявить окно прочитанным по такому ответу значит поставить курсор выше непрочитанных данных и повторять это молча каждый прогон. Реализация поступила правильно, паспорт говорил обратное. Тот же класс, что D30 и D34: паспорт расходился сам с собой, и расхождение осталось бы наследством следующему исполнителю |
| D36 | Владение методом `Store.source_usefulness(period) -> list[SourceStats]` переходит от таска 15 к таску 12. Обязанность таска 15 сузилась до расширенной разбивки по классам сигналов в команде `report` (своя агрегация в зоне `ops/`), базовый метод она вызывает готовым | оркестратор, 2026-09-07, preflight-проверка стыков перед волной 5 | Таск 12 (волна 5) заявляет в приёмке «Отчёт полезности источников за период из `store.source_usefulness`» буквально, а метод в коде — заглушка `_owner("15", …)`, и таск 15 стоит в волне 7, заблокирован таском 13, который сам заблокирован тасками 11/12/14. Контракт требовал от волны 5 то, что физически появится не раньше волны 7 — тупик очерёдности, не промах исполнителя. Данные под метод давно готовы (обязанность T07, D12-соседняя запись), расхождение в наборе чисел трёх описаний (спека / `SourceStats` / критерий T07) разрешает тот, кто метод пишет — теперь это T12 |
| D37 | `IdeaFilter` (types.py, владелец T01) дополняется полем `trust_level: TrustLevel \| None = None`. `Store.list_ideas` фильтрует по нему через `EXISTS (SELECT 1 FROM accepted_metrics WHERE idea_id = ideas.id AND trust_level = ?)`. Реализует таск 12 в рамках своей зоны (`types.py` — только это поле, дописыванием; `store.py` — добавление ветки фильтра рядом с существующими) | оркестратор, 2026-09-07, preflight-проверка стыков перед волной 5 | История 65 спеки требует буквально «фильтрую по уровню, теме, **доверию**, статусу», а `IdeaFilter` не несёт поля для уровня доверия метрики вовсе — ни `verified`/`proof`/`claimed`, никак. Тот же класс дефекта, что D08: обязанность названа в приёмке, канала в контракте нет. Поле аддитивное, значение по умолчанию `None` — старые вызовы не меняют поведения |
| D38 | Определён тип `ProjectResult` (живёт в `src/idea_scout/scaffold/__init__.py`, зона таска 14 — не в `types.py`, как `IdeaRef`/`Candidate` у T04). Поля: `idea_id: int`, `op_id: str`, `reused: bool`, `write_ok: bool`, `editor_ok: bool`, `path: str \| None`, `error: str \| None`, `suggested_name: str \| None`. Правило идемпотентности зафиксировано здесь же: `create_project` сначала зовёт `store.project_for(idea_id)` — если запись уже есть (любой `write_ok`), возвращает её как `ProjectResult(reused=True, …)` и на диск не ходит; иначе пытается создать проект. Коллизия имени папки на диске (папка есть, а записи в `store` нет) не пишет ничего в `store` и возвращает `reused=False, path=None, error=…, suggested_name=…`. Частичная или полная запись — в обоих случаях зовёт `store.save_project(...)`, `editor_ok` — отдельный, независимый от `write_ok` исход | оркестратор, 2026-09-07, preflight-проверка стыков перед волной 5 | Таблица «Границы модулей» называет `create_project(idea, cfg, op_id) -> ProjectResult`, но тип нигде не определён — тот же класс дефекта, что D08/D18. Критерии приёмки таска 14 (идемпотентность по идее, а не по `op_id`; редактор — отдельный исход; частичная запись сохраняется с диагностикой) требуют больше полей, чем несёт `ProjectRecord`, поэтому это отдельный тип, а не переиспользование |
| D39 | Читающий контракт `IdeaPreview`/`IdeaDetail` дополнен полями, без которых таск 12 не может выполнить приёмку: `IdeaPreview.market: str \| None` (уже пишется `save_score` в колонку `ideas.market`, просто не читалась), `IdeaPreview.case_date: date \| None` (максимум из `case_dates_for_idea(idea_id)` — переиспользование чужого метода, не вторая версия правила), `IdeaPreview.blocker_count: int` (`len(score.blockers)`, `0` если оценки нет), `IdeaPreview.primary_source_url: str \| None` (URL самой ранней связанной публикации). Новый метод `Store.raw_items_for(idea_id: int) -> list[RawItem]` — все источники идеи, старейший первым (join `idea_raw_links`/`raw_items`, тот же `_raw_item(row)`, что уже использует `save_raw`); `IdeaDetail.sources: list[RawItem] = field(default_factory=list)` заполняется им в `get_idea`. Реализует таск 12 (`types.py` — только эти пять полей; `store.py` — только `raw_items_for`, ветки в `_preview`/`get_idea`, миграция не нужна) | оркестратор, 2026-09-07, по возврату `BLOCKED` от исполнителя таска 12 | Границы модулей отдают `web` только чтение готовых данных из `store` («прячет: маршруты, шаблоны, токен, экранирование» — не нормализацию), а тикет 12 требует рынок первоисточника, возраст кейса, число блокеров, ссылку на первоисточник и полный список источников со снимком/атрибуцией в досье — ни одного из этих полей читающий контракт не нёс. Тот же класс дефекта, что D08/D12/D18: обязанность в приёмке есть, канала исполнения нет. Возврат `BLOCKED` — правильное поведение исполнителя, не круг ремонта (правило эскалации, AGENTS.md) |
| D40 | `Page` (types.py, владелец T01) дополняется полем `sort: Literal["priority", "level", "date"] = "priority"` (умолчание — прежнее поведение, обратной несовместимости нет). `Store.list_ideas` выбирает `ORDER BY` по нему: `priority` — `priority DESC, id DESC` как сейчас; `level` — по уровню последнего скора, `A` первой, `C` последней, идеи без скора — в конце; `date` — `first_seen_at DESC` (не `case_date`: у него бывает `NULL`, `first_seen_at` есть всегда, стабильный total order). Реализует таск 12 (`types.py` — только поле; `store.py` — ветвление `ORDER BY` в `list_ideas`, без миграции); `web` пробрасывает query-параметр `sort` | оркестратор, 2026-09-07, preflight перед ревью таска 12 | Тело тикета 12 и манифест (R38i) требуют дословно «сортировка по скору и дате», а `list_ideas(f, page)` не несёт параметра сортировки вовсе — фиксированный `ORDER BY priority DESC`, и `priority` не то же самое, что скор (это очередь обработки из `pipeline`, решение D12). Найдено оркестратором чтением диффа до отправки на ревью — тот же класс дефекта, что D37/D39, пойманный на один шаг раньше цикла ремонта |
| D41 | Три пробела приёмки таска 12, найденные оркестратором сверкой с полным списком критериев тикета до отправки на ревью. (1) `IdeaFilter.include_team: bool = False` — при `scope is None` (пользователь не выбрал масштаб явно) `list_ideas` добавляет `AND scope != 'team'`; явный выбор `scope="team"` игнорирует флаг (пользователь спросил прямо). (2) `list_ideas` при `f.archived is None` добавляет `archived ASC` первым ключом `ORDER BY` перед ключом из `page.sort` (D40) — архивные не выпадают, просто уходят вниз; явный `archived=true/false` эту добавку не делает (пользователь уже сузил список). (3) Новый тип `QueueLag(pending: int, last_success_at: datetime \| None, retry_after: datetime \| None)` и метод `Store.queue_lag(kind: JobKind) -> QueueLag` — `pending` считает `jobs` по `kind`/`state='pending'`, `last_success_at` — максимум `updated_at` по `state='succeeded'`, `retry_after` — `next_attempt_at` ближайшей `pending`-работы, если он выставлен, иначе `None`. Реализует таск 12 (`types.py` — оба поля/тип; `store.py` — ветки в `list_ideas`, новый метод `queue_lag`, без миграции) | оркестратор, 2026-09-07, preflight перед ревью таска 12 (второй проход по критериям) | (1)-(2): тикет прямо требует «Тяжёлые проекты (scope: team) по умолчанию скрыты» и «Архивные... по умолчанию внизу», реализация не различала «фильтр не задан» от «фильтр выключен» — тот же класс, что D23 для discovery. (3) Тикет требует «отставание оценки: число работ в очереди и время последнего успеха; время повтора — только если провайдер его сообщил» буквально про работы `score`, а `/api/status` использовал `unprocessed_raw_count()` — счётчик сырья конвейера (чужая очередь, T07), не работ оценки; `last_success`/`retry_after` были жёстко `None` за неимением канала. `Job.next_attempt_at` уже несёт нужную семантику ретрая (D19/D28 того же типа) — не хватало только агрегирующего метода |
| D42 | Три пробела/уточнения приёмки таска 13, найденные оркестратором чтением контракта и кода до диспетчеризации. (1) `web.create_app(store: Store, config: Config) -> FastAPI` — второй параметр добавлен; единственная правка вне зоны таска 13, тем же приёмом, что D13: `src/idea_scout/commands/serve.py` меняет ровно одну строку, `create_app(ctx.store)` → `create_app(ctx.store, ctx.config)`. (2) Заметка из раздела «Из таска 12» «по контракту `web` не импортирует соседние модули» сужена: это было узкое решение таска 12 про дублирование `_safe_url`, а не общий запрет — тело таска 13 прямо требует звать `scaffold.create_project` из `web`, и это разрешено явно. (3) Ручная постановка `dossier` мимо порога (R12.4) обязана ставить работу для **любой** идеи, включая уже обработанную автоматически (`research_state != "not_checked"`) — решение пользователя, 2026-09-07. Автоматическая постановка (`handlers/score.py:34`) и ручная используют **разные** ключи идемпотентности: автоматика оставляет `f"dossier:{idea_id}:v1"` как есть, ручная кнопка формирует свой, версионированный по моменту нажатия — например `f"dossier:{idea_id}:manual:{ts}"`, где `ts` не позволяет двум быстрым повторным нажатиям молча схлопнуться в одну и ту же работу произвольно (точный формат — на исполнителе, требование одно: результат enqueue для нажатия кнопки никогда не совпадает с уже существующей записью `jobs`, если не доказано мутационным тестом обратное). `store.enqueue` не меняется — общий метод уже принимает произвольный `unique_key` | оркестратор + пользователь, 2026-09-07, preflight перед волной 6 (таск 13) | (1)-(2): тот же класс дефекта, что D08/D12/D13/D18/D39 — обязанность («HTTP-обработчик зовёт готовый `scaffold`») названа в теле тикета буквально, а читающий контракт (`create_app`) канала для `Config`, нужного `scaffold.create_project`, не нёс, и соседняя запись интерфейса читалась как запрет вызова. (3) Спрошено пользователя напрямую, так как тело тикетов 11/12/13 связывает «мимо порога» только с порогом скора (`dossier.min_level`), а не с `research_state`, и ничего явно не говорит про повторное нажатие для уже исследованной идеи; переиспользование автоматического ключа `dossier:{id}:v1` в этом случае даёт `ON CONFLICT DO NOTHING` — молчаливый нулевой результат нажатия кнопки, что противоречит правилу 4 `AGENTS.md` («выключенный источник не звонит» — та же логика: действие человека не должно тихо ничего не делать). Пользователь выбрал «всегда работает, свой ключ» |
| D43 | **Маршрут к Reddit и X переведён на стороннего поставщика данных (Apify Actor или аналог) как основной для MVP.** Официальный Reddit OAuth и официальный X API остаются в плане, но переходят из «условия старта» в «альтернативу и запасной путь». Ступень «готовый scraper/Apify», снятая D01, возвращается — **только для Reddit и X** и только под явно принятый пользователем риск. Условие «сначала одобрение / сначала оплата тарифа» с таска 17 снимается; условие «по одному источнику за раз» сохраняется, чтобы прирост и расход оставались различимыми | пользователь, 2026-09-08, документ зафиксирован 2026-09-06: `.autopilot/2026-09-05-idea-scout--wip/reddit_x_idea_scout_handoff.md` §2 «Решение для MVP», §14 «Рекомендуемая архитектура ingestion», §15 «Итоговое решение» | Объём MVP — 5–10 тыс. постов в месяц на площадку; при нём официальный X API стоит порядка $50 за 10 тыс. постов, а готовые акторы — единицы долларов, и ожидание одобрения Reddit блокирует охват на неопределённый срок. **Основание D01 при этом не отменяется и ошибкой не признаётся:** условия X («crawling or scraping… in any form, for any purpose without our prior written consent is expressly prohibited») и требование Reddit об explicit approval остаются в силе, и сам handoff-документ говорит это дословно (§4, §10, §12). Изменилось не право, а готовность пользователя нести риск. Поэтому паспорта `x-apify.md` и `reddit.md` **не переписываются в «разрешён»** — см. D44 |
| D44 | Контрактное оформление D43, до первой строки кода таска 17. (1) `SourcePolicy.outcome` дополняется пятым значением — `"принят под риск"`: площадка сбор не разрешает, паспорт это дословно фиксирует, а сбор идёт письменным решением пользователя. Значение **не** приравнивается к «не подтверждён»/«выключен», то есть правило 4 «выключенный источник не звонит» остаётся механическим и не размывается. (2) `AcquisitionMethod` дополняется значением `"third_party_provider"`. (3) Место в архитектуре: сторонний поставщик — это `sources`, **не** `discovery`: он возвращает содержимое, которое мы намерены сохранить, а `discovery` по D04 права хранить не имеет и своё содержимое обязано терять. (4) Слой адаптеров из §14 handoff ложится на существующий контракт **без нового протокола**: один класс `Source` на юридический маршрут (`reddit_apify`, `reddit_official`, `x_apify`, `x_official`), каждый со своим статичным паспортом, все — через множественную регистрацию `SOURCES` (D17); общий формат — существующий `RawItem`, а не новый `SourceItem`. Смена актора внутри маршрута — ключ конфига, не правка кода | оркестратор, 2026-09-08, приведение контракта под D43 | `SourcePolicy` — единственное место, где живёт юридический статус канала, и оно привязано к классу источника. Один `Source` с подменяемым провайдером внутри имел бы **один** паспорт на два разных юридических режима (официальный API против стороннего сбора) — ровно та подмена, ради запрета которой написано правило «паспорт до адаптера». Разделение по маршрутам даёт свойство, которого требует §14 handoff («заменить Apify → official API без переписывания системы»), не ломая паспорта: наружу оба маршрута отдают один `RawItem`. Новый тип `SourceItem` не заводится — он дублировал бы `RawItem` и стал бы вторым источником правды |
| D45 | Одна санкционированная строка вне зоны: `src/idea_scout/commands/collect.py` проверяет устаревший паспорт перед сбором тем же предикатом `ops.schedule.is_policy_stale` и требует `--confirm-stale`, как `ops.schedule --manual`. Зона таска 17 расширяется на этот файл ровно под эту правку; тем же приёмом, что D13 и D42 | оркестратор, 2026-09-08, закрытие долга таска 15 | Правило данных «Устаревший паспорт» и обязательная проверка соответствия условиям №4 сформулированы про сбор, а не про конкретную команду. Защита стояла только в новом пути `ops.schedule --manual`, потому что зона таска 15 не давала права на `collect.py` — это дефект нарезки оркестратора, названный ревью таска 15, а не находка исполнителя. Оставить как есть значило бы иметь ручной путь, который стале-паспорт не видит, при живой команде `collect` в справке |
| D46 | TrustMRR закрыт для MVP: запрос на письменное разрешение **не отправляется**, источник остаётся выключенным до отдельного письменного разрешения. Открытое место G09 перестаёт быть ожиданием ответа и становится осознанной границей сборки | пользователь, 2026-09-08: «TrustMRR для MVP закрываем: оставить выключенным до отдельного письменного разрешения» | Требование R05 не снято и не ослаблено — уровень `verified` продолжает работать, но опирается **только** на самопубликуемые платёжные дашборды (Stripe/LemonSqueezy/Polar). Это и было следствием, названным в спеке на случай отказа: якорь `verified` у нас один, и второго в этой сборке не будет. Адаптер не пишется, паспорт `docs/sources/trustmrr.md` не меняется — его исход «выключен» верен и остаётся |
| D47 | D11 закрыт решением, а не кодом: изоляция ради фолбэка `evaluate` **не ослабляется**. При исчерпании лимита Claude скоринг ждёт в очереди с `next_attempt_at` и ничего не теряет. После D59 та же безопасная семантика применяется к research; R30 закрыт как принятая V1 граница, R29 это не затрагивает | пользователь, 2026-09-08: «оставить текущий контракт, изоляцию ради fallback evaluate не ослаблять» | Приёмка G03/R22.2 требует при оценке «ноль инструментов вообще». Codex структурно этого дать не может и по контракту обязан вернуть `unsupported_mode`. Разменять ноль инструментов на непрерывность скоринга — это ослабление именно того свойства, ради которого писалась изоляция; ожидание в очереди дешевле |
| D48 | Open Collective для MVP не используется, письменное разрешение **не запрашивается**. Класс сигналов «финансирование» остаётся незакрытым в этой сборке | пользователь, 2026-09-08: «для MVP не использовать, разрешение сейчас не запрашивать» | Условия Open Collective требуют prior written consent на сторонние приложения — тот же случай, что TrustMRR (D46). Приёмка R10 не страдает: она требует не меньше трёх классов сигналов, а подтверждены три (внедрение, проблема, внимание) из независимых семейств каналов, без «финансирования» |
| D49 | OpenCode **не** реализуется как третий `LLMProvider` внутри Idea Scout. Совместимость генерируемых проектов с OpenCode сохраняется без изменений | пользователь, 2026-09-08, после сверки контракта оркестратором | **Проверено: в R29 склеены два разных требования, и только одно про OpenCode.** Цепочка LLM-провайдеров внутри парсера (спека, Истории 42–50) называет ровно два CLI — `claude -p` и `codex exec`; OpenCode там не упомянут ни разу, и ни один тикет сборки его провайдером не делал. OpenCode живёт только во второй половине R29 — «структура генерируемого проекта одинаково читается Claude Code, Codex и OpenCode» (§Решение, История 75, §Передача работы, таск 14). Эта половина остаётся в силе и проверяется живым прогоном. Добавление третьего провайдера было бы расширением объёма, а не выполнением требования |
| D50 | В белый список `_minimal_environment()` (`llm/providers.py`) добавлена **одна** переменная — `USER`. Формулировка приёмки G03/R22.2 «минимальное окружение» этим не ослабляется и остаётся в силе | оркестратор, 2026-09-08, диагностика перед реальным прогоном; правку сделал исполнитель | Без `USER` CLI `claude` не находит свои учётные данные и отвечает `Not logged in · Please run /login` **в stdout с кодом возврата 0**. Ответ не парсится как JSON, `_outcome_from_error` не зовётся (код 0), и `complete()` возвращает `unavailable` на каждый вызов — вся цепочка LLM на машине была нерабочей, и `evaluate`, и `research`. Дефект был невидим по конструкции: штатный `pytest` по политике тестов живой CLI не зовёт, а проверку формы ответа завёл только таск 15 (R22.5) — она его и вскрыла. Имя пользователя не секрет, токенов и путей проекта не несёт; закрытость списка закреплена тестом `set(child_env) <= {...}`, красневшим на прежней реализации | 
| D51 | `run_collect(ctx, source, *, reprocess_limit: int | None = None)` — новый именованный параметр с умолчанием `None`, при котором поведение **не меняется**: доводит до очереди `_REPROCESS_LIMIT = 1000` необработанного сырья, как сейчас. При явном числе `reprocess_raw` этого прогона использует его вместо константы. `idea-scout collect` (владение T07) параметр не передаёт и не получает флага — ежедневный бэклог по-прежнему доедается полностью. Только `tests/test_ops_real.py` вызывает с малым числом | оркестратор, 2026-09-08, найдено при первом реальном прогоне | Первый реальный сквозной прогон (без правки) собрал 1071 сырой элемент с Hacker News (первый в жизни backfill пустого курсора) и молча провёл **все** их через `evidence.extract()` — реальный вызов LLM почти на каждый элемент, поскольку заголовки не матчатся дешёвым regex. Ticket-критерии T07/T09/T10 говорят «малый реальный сбор», но ничего в контракте это число не ограничивало: `_REPROCESS_LIMIT=1000` — потолок для production catch-up, не для проверочного прогона. Раздельный параметр вместо снижения самой константы: константа обслуживает суточный сбор (`ops.schedule`), которому большой бэклог — норма, а не дефект |
| D52 | Реальный сквозной прогон обязан задавать `IDEA_SCOUT_REAL_CONFIG` **отдельным** конфигом с собственным `db_path`, никогда не боевым `config/config.toml`. Заведён `config/config.real-check.toml` (в `.gitignore`, рядом с `config.toml`) — та же машина, тот же `projects_root`, отдельная база `data/idea-scout-real-check.sqlite3` | оркестратор, 2026-09-08, собственная ошибка первого прогона | Тест `test_ops_real.py` уже требовал это словами скип-сообщения («отдельной реальной конфигурацией»), но при первом запуске оркестратор подставил боевой конфиг. Реальный HN-сбор (1071 элемент, 520 идей) записался в ту самую базу, которую откроет `idea-scout serve` — production-дашборд получил бы 520 никем не проверенных строк вперемешку с будущими настоящими идеями. База пересоздана `idea-scout init` с нуля (данные были только сегодняшние, ничего настоящего не потеряно), `projects_root` не трогался — реально созданный тестом проект туда лечь может, пользователь это уже одобрил |
| D53 | `scoring/service.py::_schema()` дополняется вложенными JSON-схемами для `axes` (по каждой из четырёх осей: `value`, `rationale_ru`, `unknown_reason`) и для `blockers` (элемент — объект `{"kind", "note_ru"}`, `kind` — замкнутый список `BLOCKER_KINDS`). Прежние поля-заглушки (`"axes": {"type": "object"}`, `"blockers": {"type": "array"}`) не называли модели ни имён ключей, ни формы элемента | оркестратор, 2026-09-08, найдено первым в истории проекта живым вызовом `score()` через реальный CLI | Первый живой вызов `score()` (никогда не выполнялся реально — только на фейк-провайдере с рукописными фикстурами) вернул `axes.transferability = {"score": 0, "level": "low", "reason_ru": "..."}` вместо контрактных `value`/`rationale_ru`/`unknown_reason`, и `blockers` — список голых строк вместо списка `{"kind", "note_ru"}`. Оба поля были упомянуты в тексте промпта только человеческим языком («четыре оси... с русским обоснованием»), без единого явного имени JSON-ключа — модель разумно изобрела собственную форму, согласующуюся с видом рубрики (пороги 3/6/9), а не с типом `AxisScore`. `_axis()` и разбор `blockers` в `score()` считают это невалидным ответом и поднимают `ValueError` на каждом вызове без исключения — не флуктуация, воспроизведено дважды подряд на разных реальных идеях. Задача таска 08 (R01, схема `Score`/`AxisScore`) не отменяется: код, принимающий ответ, был и остаётся строгим ровно так, как требовал контракт; чинится то, что раньше не сообщало модели эту форму явно |
| D54 | Короткий актуальный ресёрч по §13 handoff проведён (2026-09-08/09). По его итогам таск 17 **сужен до одного маршрута**: реализуется только `x_apify`. `reddit_apify`/`reddit_official` в этом заходе таска 17 не пишутся вовсе — не «выбор не пал», а осознанная отсрочка; `docs/sources/reddit.md` в этом тикете не трогается, его исход остаётся тем, что был до ресёрча (D01, «снят»), паспорт на `принят под риск` не переводится, пока Reddit не станет отдельным заходом. Провайдер — Apify, актор **`xquik/x-tweet-scraper`** (технический id `wAusCMrm284Voaw86`, подтверждён прямым вызовом `GET https://api.apify.com/v2/acts/xquik~x-tweet-scraper`, не взят по названию на слово) — исполнитель ставит это значение умолчанием ключа конфига, а не подбирает актор сам. Целевой объём — до 10 000 постов в месяц. Официальный X API (`x_official`) в этом тикете остаётся **только архитектурным местом** из D44: класс не пишется, credentials не заводятся, доступа не запрашиваем — это по-прежнему альтернатива/запасной путь, а не то, что таск 17 подключает сейчас. Смена актора/провайдера внутри маршрута `x_apify` остаётся ключом конфига (без изменений к D44) | пользователь, 2026-09-09, по результатам ресёрча оркестратора | Ресёрч (см. отчёт в диалоге, не отдельный файл — сведён в паспорт таском 17 на шаге 0) показал, что рынок Reddit-сбора только что пережил структурный слом (закрытие неавторизованного `.json` 28.05.2026, ужесточение Reddit Responsible Builder Policy 05.06.2026, точечные иски Reddit на скрейпинг-посредников) — заходить туда первым сейчас означало бы обкатывать новый adapter layer на самой нестабильной и самой юридически заметной из двух точек входа. X через Xquik дешевле на порядок ($0.15/1000 против сравнимых Reddit-акторов от $1.50/1000, и на 1–2 порядка дешевле официального X API), не требует login/cookies и по независимым данным страницы актора имеет более широкий набор фильтров и более крупную активную базу пользователей. Официальный X API как второй `Source`-класс сейчас не добавляется — таск 17 итак ограничен «ровно один маршрут за раз», и добавление кода без выбора его к использованию было бы расширением объёма, а не выполнением требования, тем же рассуждением, что и в D49 про OpenCode |
| D55 | Закрыт контрактный долг D44: три перечисления, обещанные текстом D44, не были применены к коду. Зона таска 17 расширяется на две точечные правки вне `sources/conditional/`: (1) `src/idea_scout/types.py` — `SourceOutcome` дополняется значением `"rate_limited"`, `AcquisitionMethod` — значением `"third_party_provider"`, `PolicyOutcome` — значением `"принят под риск"`, ничего другого в файле не трогается; (2) `src/idea_scout/config.py` — секция `[sources.x_apify]` получает два типизированных поля `Config`: `sources_x_apify_actor_id: str = "xquik/x-tweet-scraper"` и `sources_x_apify_monthly_limit: int = 10000`, парсинг по образцу существующих `sources.*`-ключей, запись добавляется в `CONFIG_TEMPLATE`; ничего другого в файле не трогается. Месячный потолок (10 000 постов) не получает новой таблицы хранения: `x_apify` сам открывает `Store(config.db_path)` и `Budget(store, config.budget_monthly_cap_usd)` — тем же приёмом, что уже применяет `ops/schedule.py` (`main()`, `_run_queue()`), — и читает текущий период через существующий `store.source_usefulness(period)` (`items_fetched` за источник), сравнивая с `sources_x_apify_monthly_limit`. Протокол `Source`/`SourceFactory` (`sources/__init__.py`) не меняется ни строкой — источник получает `Store`/`Budget` не через фабрику, а строит их сам из `config.db_path`, как уже делает `ops/schedule.py` | оркестратор, 2026-09-09, закрытие долга при передаче таска 17 исполнителю | Первый же заход исполнителя (Codex) на таск 17 корректно вернул `BLOCKED`: `AcquisitionMethod`/`PolicyOutcome` в `types.py` не содержат значений из D44 (`third_party_provider`, `принят под риск`), `SourceOutcome` не содержит `rate_limited`, нужного критериям приёмки тикета; `Config` не имеет типизированных полей под `[sources.x_apify]` (секция `[sources]` в `config.py` — фиксированный список ключей, не проходной словарь); а `Source`/`SourceFactory` (`fetch(self, state) -> FetchResult`, фабрика получает только `config`) не дают источнику пути к `Store`, нужного для месячного счётчика — не по вине реализации, а потому что D43/D44 писали контракт для первого в сборке *платного* источника, а плата и месячный счётчик до этого нигде не встречались. Решение не расширяет протокол резерва T05 и не трогает `sources/__init__.py`/`ops/schedule.py`/`commands/collect.py` (кроме уже утверждённой D45 правки) — только `types.py` и `config.py`, оба точечно |
| D56 | Второй легитимный `BLOCKED` того же захода: три `CHECK`-ограничения в SQL-схеме (`migrations/001_init.sql` — `source_state.last_outcome`; `migrations/004_sources.sql` — `raw_items.acquisition_method` и `source_policies.outcome`) не пускают новые значения D44/D55 (`rate_limited`, `third_party_provider`, `принят под риск`) — база отвергает запись раньше, чем до неё доходит Python. Зона таска 17 расширяется на **один новый файл** `migrations/007_x_apify_enums.sql` (дописыванием, по правилу файла миграций: «расширение схемы после таска 01 идёт новым файлом, а не правкой старого» — существующие миграции 001–006 не трогаются ни строкой). Точная DDL и безопасная процедура пересборки трёх таблиц — `interfaces.md`, раздел «Расширение контракта от 2026-09-09 — контрактный долг миграций закрыт (D56)» | оркестратор, 2026-09-09, второй BLOCKED того же захода после D55 | Тот же класс долга, что D55, но на уровне SQL, а не Python: D44 расширил перечисления в `types.py`-прозе контракта, но параллельная SQL-схема (`migrations/004_sources.sql`, писавшаяся таском 09/10 до D43/D44) осталась с прежними `CHECK`. SQLite не даёт `ALTER TABLE ... ALTER COLUMN` для смены `CHECK` — только пересборка таблицы (создать с новой схемой → скопировать данные → удалить старую → переименовать). Три таблицы без исходящих `FOREIGN KEY` друг на друга по цепочке `raw_items` (только входящие от `signal_observations`/`metric_claims`/`evidence_findings`) — рецепт выбран так, чтобы никогда не переименовывать саму таблицу-цель чужих `REFERENCES` (SQLite ≥3.25 иначе переписывает текст FK у дочерних таблиц на промежуточное имя): новая таблица создаётся уже под временным именем, копируется, старая удаляется, и уже её место занимает переименованием новой |
| D57 | Третий легитимный `BLOCKED` того же захода: `tests/test_types.py` (владение — таск 01/08, вне зоны таска 17) держит закрытый список `ENUMS` — кортежи-снимки допустимых значений `SourceOutcome`, `AcquisitionMethod`, `PolicyOutcome` — и не обновлён вслед за D44. Зона таска 17 расширяется на три точечные правки этого файла: в словаре `ENUMS` кортеж `"SourceOutcome"` дополняется `"rate_limited"`, `"AcquisitionMethod"` — `"third_party_provider"`, `"PolicyOutcome"` — `"принят под риск"`, каждое значение добавляется в конец существующего кортежа. Больше в файле не трогается ни строки | оркестратор, 2026-09-09, третий BLOCKED того же захода после D55/D56 | Тот же долг D44, третий фронт: перечисления обещаны прозой D44 в `interfaces.md`, но три места кода их фиксируют раздельно — `types.py` (D55), SQL `CHECK` (D56), и вот теперь тест-снимок допустимых значений, который существовал ещё до D43/D44 как страховка от случайного дрейфа перечислений и не был сверен с ними при их вводе. Правка `ENUMS` — не ослабление этой страховки, а её актуализация под уже принятое расширение контракта; сам механизм проверки не меняется |
| D58 | Реальный дефект таска 17 (не контрактный долг): `Store.migrate()` (`src/idea_scout/store/store.py`) применяет `migrations/007_x_apify_enums.sql` внутри той же транзакции, что и все миграции (`apply_migrations`, `store/migrate.py`), а `PRAGMA foreign_keys=OFF` внутри открытой транзакции в SQLite — no-op. На пустых фикстурных базах (`pytest -q`) `DROP TABLE raw_items` в 007 проходит без замечаний; на базе с реальными данными (найдено при попытке реального прогона: `data/idea-scout-real-check.sqlite3`, 1198 строк `raw_items`, дочерние `signal_observations`/`metric_claims`/`product_mentions`/`idea_raw_links`) SQLite перед `DROP TABLE` делает неявный `DELETE` с проверкой внешних ключей и падает `FOREIGN KEY constraint failed`. Транзакция миграции откатилась штатно (`PRAGMA integrity_check` после — `ok`, версия схемы осталась 6, ни одна таблица не повреждена) — данные не пострадали. Правка (зона таска 17 расширяется ровно на неё): `Store.migrate()` оборачивает вызов `apply_migrations()` в `PRAGMA foreign_keys=OFF` (до открытия первой транзакции миграции, соединение в это время в autocommit) / `PRAGMA foreign_key_check` (после — нулевой результат обязателен, иначе `MigrationError`) / `PRAGMA foreign_keys=ON`. Текст самой `migrations/007_x_apify_enums.sql` не меняется — DDL был корректен, дефект в обвязке транзакций, а не в схеме таблиц. Плюс один регрессионный тест в `tests/test_migrations.py` (вне зоны таска 17 до этой точечной правки): миграция до версии 6, вставка `raw_items` + дочерней строки, затем `migrate()` до версии 7 без исключения, данные и связи сохранены | оркестратор, 2026-09-09, обнаружено при подготовке `pytest -m real` для таска 17 | Правило «исправь реальный дефект реализации по обычному циклу Codex → review» (не контрактный долг D44 — DDL миграции был согласован верно, баг в том, что `PRAGMA` внутри транзакции молча не действует, и это не могло всплыть на пустых фикстурах taska 01/09/10, только на живой базе с данными). Без этой правки `idea-scout init`, повторно запущенный на базе с накопленными реальными данными, необратимо (для того запуска) откажет на миграции 007 — обнаружено до того, как это затронуло что-либо, кроме локальной песочницы real-check |
| D63 | Final acceptance V1 проведён отдельным чатом-ревьюером 2026-09-11 на HEAD `09ab69c`. Единственная закрытая позиция — R29 (был `in-ticket`), переведён в `done` живой проверкой всех трёх CLI на реально сгенерированном скаффолде. Остальные 46 требований приняты по существующему evidence (manifest, review-log, `pytest -q`) плюс точечный live-смок дашборда на `config/config.qa-dashboard.toml` (список идей, карточка, атрибуция, статус источников — все отдали живые данные). Новых дефектов не найдено | оркестратор (независимый final acceptance чат), 2026-09-11 | Задача пользователя — не повторять code review, а закрыть R29 и принять V1. R29 был единственным требованием без живого доказательства; остальное уже прошло два круга ревью (2026-09-10 → 2026-09-11) с нулём открытых находок |
